---
id: deep_research
name: 深度调研（DeepResearch）
description: 多模型并行多角度调研 + 汇总成文 + 跨模型交叉审核 + 迭代修正，产出高质量、多维度、低偏见的调研报告
version: 1
enabled: true

# 触发准入：命中任一关键词即触发（配合 trigger_min_length 防误触）
triggers:
  # 成文类需求
  - 调研
  - 研究报告
  - 研究一下
  - 行业分析
  - 市场分析
  - 竞品分析
  - 竞争分析
  - 深度总结
  - 深度分析
  - 深入分析
  - 全面分析
  - 对比分析
  - 可行性分析
  - 复盘
  # 方案设计类需求
  - 方案
  - 制定
  # 多视角/决策类需求
  - 多角度
  - 多维度
  - 多视角
  - 利弊
  - 优劣
  - 正反
  - 该不该
  - 要不要
  - 值不值得
trigger_min_length: 8

# 执行参数
params:
  min_dimensions: 2      # 最少调研维度
  max_dimensions: 5      # 最多调研维度
  call_timeout_ms: 400000  # 单次模型调用外层超时
---

# 深度调研 Skill（DeepResearch）

供所有 Agent/模型使用的标准化技能：多模型并行多角度调研 + 汇总成文 + 跨模型交叉审核 + 迭代修正。
纯事件 + 回调驱动，无轮询无阻塞；不依赖能力画像，仅按「角色分工 + 参与状态」调度。

## Agent 角色分工

- **主对话汇总角色（主模型）**：维度拆解、初稿汇总撰写、审核意见迭代修正，全程收口
- **多角度调研角色（调研模型）**：至少 2 个不同在线模型，差异化多角度独立调研
- **独立审核角色（审核模型）**：跨模型强隔离审核——优先本轮未参与任何环节的空闲模型；
  全员参与时禁止撰写主模型自审，从调研模型中轮换选取

## 5 步执行流程

1. **维度拆解**（主模型）：拆解独立、不重叠、互补的调研方向（2-5 个），写入黑板
2. **并行调研**（≥2 个调研模型）：随机分配、强制视角差异化，各维度独立快照互不覆盖
3. **汇总成文**（主模型）：去重、互补、纠偏、整合冲突观点，输出完整初稿
4. **独立审核**（审核模型，强隔离）：输出漏洞清单/逻辑/片面性/表述/缺失维度/建议/风险
5. **迭代终稿**（主模型）：逐条对照审核意见修订，输出终稿 + 可溯源材料

## 提示词模板

引擎从下方 `prompt` 代码块加载各步骤提示词模板（`{{变量}}` 占位），修改模板无需改代码。

```prompt dimension_split
你是多智能体平台的深度调研规划模型，负责为一次多模型多角度调研拆解思考维度。

要求：
1. 针对用户需求拆解出「独立、不重叠、互补」的调研维度（至少{{min_dimensions}}个，复杂任务{{min_dimensions}}-{{max_dimensions}}个）
2. 维度之间视角必须差异化（如：正向优势/反向风险/行业视角/用户视角/落地视角等）
3. 每个维度定义：调研目标、分析视角、输出格式、论证要求

输出纯 JSON 数组，不要任何其他文字或 markdown 围栏，格式：
[{"name":"维度名称","goal":"调研目标","perspective":"分析视角","format":"输出格式与论证要求"}]

用户需求：{{user_query}}
```

```prompt dimension_research
你是深度调研中的独立调研模型，负责以下调研维度（只做本维度，不要涉及其他视角）：
维度名称：{{dim_name}}
调研目标：{{dim_goal}}
分析视角：{{dim_perspective}}
输出/论证要求：{{dim_format}}

要求独立调研、给出观点与依据，输出本维度的调研结论（论据、信息、观点），不要写其他维度的内容。

用户需求：{{user_query}}
```

```prompt draft
你是深度调研的主汇总模型，负责把多个模型的多角度独立调研结果整合为一篇完整报告。

要求：
1. 读取全部调研素材：去重、互补、纠偏、整合冲突观点
2. 按调研报告标准结构撰写：标题、背景/概述、多维度分析、综合结论与建议
3. 逻辑串联、观点论证、内容落地，输出完整初稿（不要写审核意见，终稿另有机会修订）

用户需求：{{user_query}}

=== 多模型多角度调研素材 ===
{{research_sections}}
```

```prompt review
你是深度调研的独立审核模型（{{reviewer}}），你没有参与本次调研与撰写，请完全独立、客观地审核以下初稿，不继承任何撰写者的结论偏好。

审核维度：合规性、逻辑性、完整性、片面性、事实风险。
输出审核报告，必须包含以下清单（逐条列出，没有问题的项写「无」）：
1. 漏洞清单（论证漏洞/事实存疑）
2. 逻辑问题
3. 片面性问题（是否只呈现单方观点）
4. 表述问题
5. 缺失维度（调研素材中有但初稿未覆盖的内容）
6. 优化建议
7. 风险点

用户需求：{{user_query}}

=== 各模型调研素材（对照用） ===
{{research_sections}}

=== 待审核初稿 ===
{{draft}}
```

```prompt final
你是深度调研的主汇总模型，独立审核模型已对初稿提出审核意见，请逐条对照意见修订。

要求：
1. 逐条修正内容漏洞、补充缺失维度、优化逻辑、平衡观点、修正片面性
2. 完成迭代改写，输出最终定稿（完整全文，不是差异说明）
3. 保留初稿中经得起推敲的内容，不要为改而改

用户需求：{{user_query}}

=== 初稿 ===
{{draft}}

=== 审核意见 ===
{{review}}

=== 调研素材（补充论据用） ===
{{research_sections}}
```

```prompt final_no_review
你是深度调研的主汇总模型，请对以下初稿做最终定稿：自查逻辑与完整性，微调后输出完整终稿全文。

用户需求：{{user_query}}

=== 初稿 ===
{{draft}}
```

## 黑板存储

- `blackboard:skill:deep_research:{trace_id}:dimensions` 维度清单
- `blackboard:skill:deep_research:{trace_id}:sub_research` 各模型独立调研结果（Hash，维度名为 field）
- `blackboard:skill:deep_research:{trace_id}:draft` / `:review` / `:final` 初稿 / 审核报告 / 终稿

## 事件清单

SKILL_DEEP_RESEARCH_START_EVENT → RESEARCH_DIMENSION_READY_EVENT → MULTI_RESEARCH_ALL_FINISH_EVENT → RESEARCH_DRAFT_FINISH_EVENT → REVIEW_RESULT_FINISH_EVENT → SKILL_DEEP_RESEARCH_COMPLETE_EVENT
