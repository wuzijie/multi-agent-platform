node_modules/
logs/
tasks/
.DS_Store
*.pid
workspace/
/blackboard/
# 记忆运行时数据（会话摘要 / 长期记忆 / 日志），不入库
/agent_memory/
# 密钥配置不入库（api_keys.yaml 含真实 API key，曾在历史中暴露，建议轮换）
config/api_keys.yaml
# agents/model_cli.log 是请求/响应日志，需要被 git 追踪
