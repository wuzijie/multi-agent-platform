# 多Agent平台协作Prompt管理方案

# 一、方案概述

本方案旨在标准化多Agent协作平台的Prompt分层管理、上下文组装、专项Prompt调度、模型请求传参全流程，解决多智能体会话上下文混乱、静态/动态规则混杂、工具与MCP配置不统一、记忆提取与上下文压缩无规范、子Agent角色调度无标准等问题。通过**固定底层内容、长效知识沉淀、跨轮次状态留存、短期会话增量更新、专项Prompt独立管控**的分层架构，实现所有Agent会话上下文的标准化组装、高效迭代、精准调用，保障多Agent协作的稳定性、一致性与高效性。

本方案核心适用范围：平台主Agent、各业务子Agent、记忆提取Agent、上下文压缩Agent、会话记忆管理Agent等全角色智能体的Prompt配置与上下文请求流程。

# 二、整体上下文分层架构（优先级从高到低）

平台所有模型请求的上下文，统一按照「固定底层内容→长期知识内容→跨轮次留存内容→短期会话增量内容」四层固定顺序组装，同时配套独立的专项Prompt资源池，按需挂载调用，杜绝内容顺序混乱、无效内容冗余、核心规则被覆盖等问题。完整层级排序如下：

1. **第一层：全局固定底层内容（上下文最前端，永久固定、优先加载）**

2. **第二层：长期知识内容（全局固定内容后置，长效不变、项目全生命周期生效）**

3. **第三层：跨轮次留存内容（中长期留存，跨会话、跨轮次复用）**

4. **第四层：单次会话增量内容（上下文最后端，单次会话临时生效）**

# 三、各层级内容详细定义与管理规范

## （一）第一层：全局固定底层内容（静态\+动态双模块）

该层级为平台运行的核心基础规则，所有Agent会话默认置顶加载，不可被会话内容覆盖、删减。分为**静态固定内容**和**动态运行内容**两部分，动态内容由项目运行时实时采集更新。

### 1\. 静态System Prompt（永久固定，全局通用）

无运行时变量，平台初始化即固定，所有Agent通用基础规则，核心包含：

- **身份信息**：多Agent平台定位、主Agent/通用Agent基础身份、服务定位、能力边界、协作定位；

- **基础工作原则**：平台通用工作准则、回答规范、协作原则、安全合规规则、上下文使用原则、工具调用底线、多Agent分工协作基础要求；

- **通用约束规则**：输出格式约束、逻辑严谨性要求、拒绝规则、信息真实性校验规则、跨Agent通信基础规范。

### 2\. 动态运行固定内容（项目运行时实时获取、动态更新）

随项目、环境、运行状态变化实时刷新，每次模型请求自动拉取最新内容，覆盖内容包括：

- **当前启用工具定义**：本次会话已激活的所有工具列表、工具参数、调用规则、入参出参格式、工具权限、工具使用场景约束；未启用工具不加载，避免冗余；

- **MCP相关配置**：MCP Server运行指令（instructions）、MCP通信协议、服务地址、权限配置、数据交互规则、上下游服务联动规则；

- **模型运行时信息**：当前会话使用的基础模型版本、模型能力参数、上下文最大长度、温度值、生成长度、采样参数等运行时配置；

- **项目运行状态标识**：项目运行环境、调试/正式模式、权限等级、会话身份标识。

## （二）第二层：长期知识内容（长效留存）

该层级为项目、团队的沉淀性知识，在项目生命周期内固定不变，长期挂载于上下文，无需每轮更新，仅在知识迭代时手动更新，放置于固定底层内容之后。核心内容：

- **项目知识**：项目背景、业务目标、核心流程、技术架构、项目规范、业务术语、项目约束、需求基线、核心功能说明；

- **团队知识**：团队协作规范、角色分工、业务处理习惯、过往最优实践、通用问题解决方案、团队输出标准；

- **公共知识库**：平台通用业务常识、固定话术、标准流程、错误处理规范。

## （三）第三层：跨轮次留存内容（中长期复用）

该层级为跨会话、跨轮次的状态与记忆内容，是会话连续性的核心支撑，放置于长期知识之后、单次会话增量内容之前，支持持久化留存与按需加载。核心分类：

### 1\. 用户级上下文内容

绑定当前项目目录与用户维度的固定配置文件、状态文件，核心为项目目录下的 **project\.md**，包含项目个性化配置、用户自定义规则、项目专属Prompt约束、业务定制要求、会话偏好设置。

### 2\. 系统级上下文内容

平台系统运行状态快照，实时采集、跨轮次留存，核心包含：git status代码状态快照、文件目录结构、系统配置快照、服务运行状态、历史操作记录快照、资源占用状态等系统层级信息。

### 3\. 跨轮次记忆内容

会话长期记忆、用户偏好记忆、历史关键结论、历史核心交互信息、任务进度留存、未完成任务记录、关键决策记录，实现多轮会话上下文不丢失、任务可持续迭代。

## （四）第四层：单次会话增量内容（短期临时）

该层级为**单次会话独立产生的临时内容**，仅当前会话生效，会话结束后不默认留存，放置于整个上下文的最末尾。核心内容包括：当前轮次用户提问、当前Agent中间推理过程、工具调用返回结果、临时计算数据、实时交互对话、临时任务指令、本轮次输出草稿等增量信息。

# 四、专项Prompt独立管理体系

针对多Agent平台细分场景，单独搭建**专项Prompt资源池**，与通用上下文分层体系解耦，按需调用、独立迭代、单独版本管理，避免通用上下文臃肿，同时保障细分场景的专业性。所有专项Prompt统一分类存储、单独配置版本号、支持启停切换。具体分类如下：

## 1\. 子Agent专属Prompt（角色调度专用）

平台拆分出的所有业务子Agent独立配置专属内容，包含：子Agent专属System Prompt \+ 子Agent独立工具定义。

内容包含：子Agent专属身份、职责边界、工作原则、输出规范、专属可调用工具、工具权限、协作对接规则、专属业务约束，子Agent被触发时，自动挂载该套专属Prompt，叠加全局通用底层内容使用。

## 2\. 上下文压缩专项Prompt

专门用于超长会话上下文的精简、去重、降噪、关键信息保留的专属Prompt，定义压缩规则：冗余内容删除标准、核心记忆保留规则、对话摘要生成规则、上下文长度阈值、压缩格式规范，由上下文压缩Agent专属调用。

## 3\. 通用角色Agent专项Prompt

平台各类固定角色Agent的标准化Prompt，包含各角色专属System Prompt \+ 匹配工具定义，如：任务拆解Agent、代码开发Agent、文档整理Agent、问题排查Agent、协作调度Agent等，实现角色能力标准化、输出统一化。

## 4\. 会话记忆（Session Memory）专项Prompt

用于会话临时记忆的存储、更新、覆盖、清除规则定义，规范单会话内记忆留存范围、临时记忆过期规则、会话内信息继承逻辑，保障单次会话交互的连贯性。

## 5\. 记忆提取（Extract Memories）专项Prompt

专门用于从历史对话、跨轮次数据中提取有效长期记忆的专属Prompt，定义记忆提取维度、关键信息判定标准、无效信息过滤规则、记忆结构化存储规则，支撑跨轮次记忆的精准沉淀。

# 五、模型请求上下文组装全流程

平台每次向大模型发起请求前，统一按照固定顺序自动组装上下文，结合场景按需挂载专项Prompt，标准化传参，流程如下：

1. **加载全局固定底层内容**：优先载入静态System Prompt，实时拉取动态内容（启用工具、MCP指令、当前模型配置）；

2. **加载长期知识内容**：载入项目知识、团队沉淀知识、公共长效知识库内容；

3. **加载跨轮次留存内容**：依次载入用户级project\.md配置、系统状态快照、历史跨轮次记忆内容；

4. **按需挂载专项Prompt**：根据当前激活Agent角色、当前执行场景（压缩上下文、提取记忆、子Agent调度等），自动挂载对应专项Prompt；

5. **追加短期会话增量内容**：载入本轮用户提问、实时交互数据、工具返回结果等临时内容；

6. **上下文预处理**：自动去重、降噪、超长截断/压缩、格式校验；

7. **发起模型请求**：将组装完成的完整上下文传入模型，完成推理生成。

# 六、Prompt版本与运维管理规范

## 1\. 版本管理

所有通用分层Prompt、专项Prompt统一做版本管控，迭代更新记录版本号、更新时间、更新内容、适用场景，支持版本回滚。

## 2\. 内容更新规则

- 静态固定内容：仅平台版本迭代时更新，日常会话不变更；

- 动态固定内容：每次模型请求实时自动刷新；

- 长期知识内容：人工审核后手动更新，不自动变更；

- 跨轮次记忆内容：由记忆提取Agent自动迭代\+人工校验；

- 短期增量内容：单次会话有效，会话结束自动清空；

- 专项Prompt：按场景独立迭代，不影响全局上下文。

## 3\. 权限管控

底层固定Prompt、专项核心Prompt仅管理员可编辑；项目/团队知识支持项目负责人编辑；会话增量、记忆内容由平台自动调度，普通用户无编辑权限。

# 七、方案价值

- **标准化**：统一多Agent所有会话的上下文结构与Prompt调度逻辑，解决杂乱无章的问题；

- **高效化**：分层存储、按需加载，区分长效/短期/固定/专项内容，减少冗余计算；

- **可迭代**：各类Prompt独立管理、版本可控，支持场景化定制与快速迭代；

- **高稳定**：核心规则置顶固定，避免会话交互覆盖基础约束，保障多Agent协作一致性；

- **强连贯**：跨轮次记忆与系统状态留存，实现长会话、多任务、多Agent协同的连续性。

> （注：部分内容可能由 AI 生成）
