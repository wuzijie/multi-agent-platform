# Multi\-Agent 深度调研 Skill（标准化技能定义）

## 一、Skill 基础信息

**Skill 名称**：深度调研（DeepResearch）

**Skill 唯一标识**：SKILL\_DEEP\_RESEARCH

**技能定位**：多模型并行多角度调研 \+ 汇总成文 \+ 跨模型交叉审核 \+ 迭代修正，用于产出高质量、多维度、低偏见的调研报告/分析方案/深度思考文档。

**架构适配**：纯事件\+回调驱动、通用模型调度、Redis 黑板存储中间结果、多模型隔离协作。

## 二、触发条件（精准准入规则）

满足**任意一条**自动触发本 Skill，由 Planner Agent / 调度器事件识别准入：

1. 用户明确要求：撰写调研报告、行业分析、竞品分析、深度总结、方案调研、复盘分析等成文类需求。

2. 用户任务需要**多角度、多维度、正反、利弊、优劣、多方视角**思考，单一模型思考存在片面性。

3. 任务属于复杂决策类、论证类、资料整合类、观点输出类内容，需要规避单点幻觉与思维盲区。

**禁止触发场景**：简单问答、指令执行、单步工具调用、无需多角度论证的短内容生成。

## 三、Agent调度与角色分配规则（无能力画像模式）

本Skill **不依赖任何Agent能力画像、能力标签、专项能力匹配机制**，摒弃能力筛选逻辑，仅通过任务角色分工 \+ 任务参与状态完成模型调度，所有在线空闲通用模型均可参与协作。系统仅区分执行角色，不区分模型能力差异。

- **主对话汇总角色（主模型）**：负责维度拆解、初稿汇总撰写、审核意见迭代修正，承担任务全程收口工作

- **多角度调研角色（调研模型）**：至少分配2个及以上不同空闲模型，负责差异化多角度独立调研，保证视角多元化

- **独立审核角色（审核模型）**：负责初稿内容合规性、逻辑性、完整性审核，严格执行跨模型隔离审核规则

**审核兜底隔离规则（核心强制规则）**：

严格按照任务参与状态分配审核模型：优先选取本轮**未参与任何环节的空闲模型**执行审核；若所有在线模型均已参与调研/撰写，严格禁止**撰写主模型自审**，从已参与的调研模型中轮换选取其他模型执行审核，完全杜绝自审闭环问题。

## 四、完整标准化执行步骤（5步闭环，严格对齐需求）

本 Skill 所有步骤**全部事件驱动**：每一步完成发布完成事件 → 触发下一步回调，无轮询、无阻塞。

### Step 1：确定思考方向（维度拆解）

**执行角色**：主对话模型 / 规划 Agent

**触发事件**：SKILL\_START\_EVENT

**执行内容**：

- 针对用户需求，拆解本次调研的**独立、不重叠、互补的多角度思考方向**（至少2个维度，复杂任务3\-5个维度）。

- 定义每个调研维度的：调研目标、分析视角、输出格式、论证要求。

- 将维度清单写入 Redis 黑板快照，生成多模型并行调研任务 DAG。

**回调事件**：RESEARCH\_DIMENSION\_READY\_EVENT（触发多模型并行调研）

### Step 2：多模型多角度并行调研（至少双模型差异化调研）

**执行角色**：2个及以上不同调研模型（强制多模型隔离）

**触发事件**：RESEARCH\_DIMENSION\_READY\_EVENT

**执行规则（核心）**：

- 调度器随机分配**不同空闲通用模型**承接不同调研维度，强制拆分视角，保证各模型调研方向完全差异化、无重叠。

- Model A 负责正向/优势/现状维度；Model B 负责反向/风险/短板维度；可扩展 Model C 行业视角、用户视角、落地视角等。

- 各模型**独立调研、互不干扰**，单独输出各自维度的调研论据、信息、观点、依据。

**落库动作**：所有独立调研结果分别写入 Redis 黑板独立快照，避免覆盖。

**回调事件**：MULTI\_RESEARCH\_ALL\_FINISH\_EVENT（所有调研模型全部完成后触发）

### Step 3：主模型汇总、整合、撰写成文

**执行角色**：主对话汇总模型（MainModel）

**触发事件**：MULTI\_RESEARCH\_ALL\_FINISH\_EVENT

**执行内容**：

- 读取黑板中所有多角度调研结果，去重、互补、纠偏、整合冲突观点。

- 按照调研报告/方案标准结构，完成完整文章撰写、逻辑串联、观点论证、内容落地。

- 输出**初版完整成品内容**，写入黑板待审核快照。

**回调事件**：RESEARCH\_DRAFT\_FINISH\_EVENT（触发独立审核流程）

### Step 4：跨模型独立审核（强隔离规则）

**执行角色**：独立审核模型（严格隔离规则）

**触发事件**：RESEARCH\_DRAFT\_FINISH\_EVENT

**隔离审核规则（你指定核心规则落地）**：

1. 优先选择：**本轮未参与调研、未参与撰写的空闲 Agent/模型** 进行审核。

2. 若所有模型均已参与本轮任务：禁止使用**本次撰写主模型**，从调研模型中轮换选取非主写模型审核。

3. 审核完全独立，不继承之前任何模型的结论偏好，客观纠错。

**审核输出内容**：漏洞清单、逻辑问题、片面性问题、表述问题、缺失维度、优化建议、风险点。

**回调事件**：REVIEW\_RESULT\_FINISH\_EVENT（输出审核报告，触发迭代修改）

### Step 5：主模型根据审核意见迭代修正，输出终稿

**执行角色**：主对话汇总模型

**触发事件**：REVIEW\_RESULT\_FINISH\_EVENT

**执行内容**：

- 逐条对照审核意见，修正内容漏洞、补充缺失维度、优化逻辑、平衡观点、修正片面性。

- 完成迭代改写，生成**最终定稿版本**。

- 整合：多维度调研素材 \+ 审核修正记录 \+ 终稿内容，形成完整可溯源成果。

**闭环事件**：SKILL\_FULL\_COMPLETE\_EVENT（任务完结，输出最终内容）

## 五、本Skill专属事件清单（融入全局事件体系）

- **SKILL\_DEEP\_RESEARCH\_START\_EVENT**：深度调研技能启动

- **RESEARCH\_DIMENSION\_READY\_EVENT**：思考维度拆解完成，可并行调研

- **MULTI\_RESEARCH\_ALL\_FINISH\_EVENT**：多模型多角度调研全部完成

- **RESEARCH\_DRAFT\_FINISH\_EVENT**：初稿撰写完成，等待审核

- **REVIEW\_RESULT\_FINISH\_EVENT**：独立审核完成，产出整改意见

- **SKILL\_DEEP\_RESEARCH\_COMPLETE\_EVENT**：深度调研全流程完结

## 六、Agent调度与兜底执行策略（无能力画像通用规则）

1. **调研阶段**：不区分模型能力，随机分配至少2个不同空闲模型独立承接各调研维度，强制保证视角多元、结论互补。

2. **撰写阶段**：选取单一空闲通用模型作为主汇总模型，统一收口全文结构与内容。

3. **审核阶段强隔离**：优先未参与本轮任务的空闲模型；全员参与则**绝对禁止撰写模型自审**，轮换其他参与模型执行审核。

4. **负载兜底**：无空闲模型时，排队等待节点释放，保障任务有序执行、不卡顿、不丢失。

## 七、Redis黑板存储结构（Skill 专属）

- `blackboard:skill:deep_research:{trace_id}:dimensions`：调研思考方向维度清单

- `blackboard:skill:deep_research:{trace_id}:sub_research`：各模型独立调研结果

- `blackboard:skill:deep_research:{trace_id}:draft`：撰写初稿

- `blackboard:skill:deep_research:{trace_id}:review`：审核报告与问题清单

- `blackboard:skill:deep_research:{trace_id}:final`：最终定稿内容

## 八、Skill 核心价值

- 解决单模型思维片面、幻觉、视角单一问题；

- 多模型并行差异化调研，论据更全面、客观；

- 跨模型交叉审核，杜绝自写自审的闭环缺陷；

- 全流程事件驱动、可溯源、可复盘、可迭代；

- 完美兼容前文「消息总线\+Redis黑板\+纯事件回调驱动」整套架构，无能力依赖，适配所有通用模型集群。

> （注：部分内容可能由 AI 生成）
