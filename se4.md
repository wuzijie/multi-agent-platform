以下是按照你的修改要求彻底简化后的 V2.0 开发方案：

---

# 多智能体（Multi-Agent）协作平台三期开发方案设计 V2.0

**版本：V2.0**　　**日期：2026-08-11**　　**状态：定稿**

---

## 一、项目概述

本文档基于《多智能体（Multi-Agent）协作平台产品需求说明》，制定分三期交付的开发方案。平台定位为**本地化运行的多智能体协作工具**，基于本地配置文件驱动，无需外部数据库、消息中间件、账户体系等重型基础设施，通过多模型智能体分工协作、相互评审、动态纠错、愿景守护，实现复杂开发及内容生产任务的标准化、高质量落地。

### 1.1 三期总体策略

**第一期（MVP 基础能力期，4-6 周）**：单 Agent 任务执行闭环，本地文件配置驱动，任务复杂度由默认模型自动判别。

**第二期（协作增强期，6-8 周）**：多 Agent 并行协作、9 步标准化协作流程、跨模型交叉评审、状态管控、特性清单追踪。

**第三期（智能闭环期，4-6 周）**：愿景守护、调度策略优化、任务效能分析与报表。

### 1.2 技术架构总览

平台采用四层本地架构：

**接入层**——CLI 用户交互界面（命令行 + 可选 Web 本地服务），用户通过自然语言输入任务需求。

**调度层**——Task Orchestrator + Agent Scheduler，任务解析 → 画像生成 → 流程编排 → Agent 匹配 → 状态管理，全流程基于本地文件驱动。

**执行层**——Multi-Model CLI Runtime，通过统一 CLI 抽象适配层调用各模型厂商 CLI，线程隔离并行运行。**CLI 抽象层对各模型厂商 CLI 的输入输出做统一标准化定义**，项目运行时所有 Agent 均通过该抽象接口通信，屏蔽不同厂商 CLI 的协议差异。

**状态层**——Local File State Store + File Event Bus，任务数据、Agent 状态、协作流程信息、评审意见、快照等全部使用本地结构化文件（JSON / Markdown）存储。Event Bus 基于文件系统实现组件间事件通知，无需外部 MQ 中间件。

### 1.3 本地文件存储规范

项目运行目录结构：

```
project-root/
├── config/
│   ├── agents.yaml          # Agent 角色配置（启动时加载，运行期只读）
│   ├── git.yaml              # Git 仓库配置（默认仓库 + 支持用户修改）
│   └── task_defaults.yaml    # 任务默认参数（超时、重试次数等）
├── tasks/
│   ├── index.json            # 任务索引（所有任务列表与当前状态）
│   └── {task_id}/            # 每个任务独立目录
│       ├── task.json         # 任务详细信息与进度
│       ├── snapshots/        # 各轮迭代快照
│       │   ├── round_1.json
│       │   └── round_2.json
│       ├── reviews/          # 评审意见文件
│       │   ├── agent_a.md
│       │   └── agent_b.md
│       └── outputs/          # 任务产出文件（代码、方案等）
├── features/                 # 特性清单（每个特性一个 .md 文件）
│   ├── INDEX.md              # 特性总索引
│   ├── feat_001.md
│   └── feat_002.md
├── logs/
│   ├── agents/               # Agent 运行日志
│   └── events/               # Event Bus 事件日志
└── workspace/                # Git worktree 工作区
```

### 1.4 CLI 抽象层设计

各模型厂商（Claude / Kimi / DeepSeek / Qwen）提供的 CLI 工具在输入输出格式、参数风格、错误码定义上存在差异。项目在接入每个模型 CLI 前，先定义统一的 CLI 抽象接口：

**统一输入格式**：

```json
{
  "task_id": "uuid",
  "role": "executor | reviewer | guardian",
  "context": "任务上下文与背景",
  "instruction": "具体执行指令",
  "input_files": ["file_paths"],
  "max_tokens": 4096
}
```

**统一输出格式**：

```json
{
  "task_id": "uuid",
  "status": "success | failed | timeout",
  "content": "产出文本内容",
  "output_files": ["file_paths"],
  "error": { "code": "xxx", "message": "xxx" },
  "tokens_used": 1234,
  "duration_ms": 5000
}
```

**适配器职责**：每个模型 CLI 对应一个适配器实现，负责将统一输入翻译为对应 CLI 的原生参数格式，并将原生输出标准化为统一输出格式。运行时所有 Agent 调度逻辑仅依赖抽象接口，不直接调用各厂商原始 CLI。

---

## 二、第一期：MVP 基础能力期（预计 4-6 周）

核心目标：搭建项目骨架，实现单 Agent 任务闭环，建立本地配置驱动、CLI 抽象层、Git worktree 隔离、特性清单追踪机制。

### 2.1 核心功能范围

#### 2.1.1 本地配置启动机制

- 项目启动时读取 `config/agents.yaml` 加载所有 Agent 角色配置（角色名称、绑定模型 CLI、能力标签、优势、劣势、适用场景），**运行期间角色数量与配置固定**。
- 读取 `config/git.yaml` 加载 Git 仓库配置，内置一个默认仓库地址，**支持用户编辑配置文件变更仓库**。
- 读取 `config/task_defaults.yaml` 加载任务默认参数。

#### 2.1.2 CLI 抽象层实现

- 定义统一 CLI 输入输出标准化接口（1.4 中定义的结构）。
- 一期仅实现克劳德（Claude Code CLI）适配器，其余模型适配器二期开发。
- 运行时所有 Agent 调用均通过抽象接口，不直接访问原始 CLI。

#### 2.1.3 默认承接 Agent（对应需求 1.1）

- 实现克劳德 Agent 作为平台默认承接智能体。
- 基于 CLI 抽象层与 Agent 建立通信。

#### 2.1.4 任务复杂度自动判别（对应需求 1.1）

- 任务复杂度判断**完全交给默认模型（克劳德）自行判别**：接收用户输入后，克劳德自动评估任务为简单或复杂，评估结果写入 `task.json` 的 `complex_flag` 字段。
- 简单任务：克劳德独立执行，直接输出结果。
- 复杂任务：一期复杂任务先标记不执行多 Agent 协作，输出提示「完整多 Agent 协作将于二期上线」；`task.json` 中记录 `complex_flag: true` 便于二期迁移处理。

#### 2.1.5 单 Agent 任务全流程闭环

- 任务解析 → 复杂度判别 → Agent 执行 → 结果返回至本地输出目录。
- 任务状态机：执行中 → 执行结束 / 执行失败。
- 任务信息全部存入 `tasks/{task_id}/task.json`。
- 任务索引更新至 `tasks/index.json`。

#### 2.1.6 代码开发与 Git 隔离（对应需求 七）

- 基于 `config/git.yaml` 中配置的默认仓库，创建独立 worktree 进行代码开发。
- worktree 命名规则绑定 `task_id`，目录位于 `workspace/{task_id}/`。
- worktree 生命周期管理：创建、清理。
- 定时清理：自动删除任务状态为失败 / 终止且 7 天以上的 worktree。

#### 2.1.7 特性清单追踪机制

- `features/INDEX.md` 维护全部特性总索引，每条特性链接至独立 `.md` 文件。
- 每个特性文件包含标准字段：

```markdown
# 特性名称

- **描述**：xxx
- **状态**：规划中 | 开发中 | 已完成 | 已暂停
- **开发进度描述**：当前已完成内容与剩余工作
- **关联任务**：task_id_xxx
- **创建时间**：2026-08-11
- **最后更新**：2026-08-11
```

- 启动特性开发时自动生成该文件，开发过程中按进度修改文件内容。
- 三期效能分析报表将读取 `features/` 目录进行统计。

#### 2.1.8 File Event Bus

基于文件系统的简易事件总线：

- 各模块向 `logs/events/` 写入事件文件（格式：`{timestamp}_{event_type}.json`）。
- 消费者模块通过轮询或文件变更监听读取事件文件。
- 事件类型包括：任务状态变更、Agent 状态变更。
- 一期实现单进程内模块间通知即可，不依赖外部 MQ。

### 2.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| 配置加载器 | 读取 YAML 本地文件 | 启动时正确加载 agents / git / task_defaults 配置 |
| CLI 抽象层 | 统一接口 + 克劳德适配器 | 输入输出标准化，底层 CLI 差异已屏蔽 |
| Agent Runtime | CLI 抽象接口调用 | 单 Agent 进程启停正常 |
| Task Orchestrator | 状态机 + 本地文件存储 | 任务创建 → 执行 → 结束全链路可追踪 |
| Git Worktree Manager | Git CLI 封装 | worktree 创建 / 清理，定时回收 |
| File Event Bus | 文件系统事件写入 | 模块间状态变更事件可达 |
| 特性清单系统 | Markdown 模板 + 索引 | 特性文件自动生成，进度实时更新 |
| CLI 用户界面 | 命令行交互 | 输入任务需求、查看执行结果 |

### 2.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 项目骨架 | 第 1 周末 | 1 周 | 目录结构、配置加载、CLI 抽象层接口定义 |
| M2: 单 Agent 链路 | 第 3 周末 | 2 周 | 克劳德适配器、任务复杂度判别、简单任务闭环 |
| M3: 工作区 + 特性清单 | 第 5 周末 | 2 周 | Git worktree 隔离、特性清单系统、File Event Bus |
| M4: 验收 + 修复 | 第 6 周末 | 1 周 | 端到端联调、Bug 修复、功能验收 |

### 2.4 第一期量化验收指标

- 本地配置正确加载，启动无报错。
- 克劳德 Agent 通过 CLI 抽象层正常通信。
- 任务复杂度自动判别正确，复杂任务标记不误判。
- 简单任务端到端执行成功，结果写入指定输出目录。
- Git worktree 创建 / 清理成功率 100%。
- 特性清单文件按模板自动生成，字段齐全。
- Event Bus 事件文件写入格式正确。

---

## 三、第二期：协作增强期（预计 6-8 周）

核心目标：引入多 Agent 并行协作、9 步标准化协作流程、跨模型交叉评审、全维度状态管控。

### 3.1 核心功能范围

#### 3.1.1 多 Agent 并行运行机制（对应需求 二）

- 实现四大 Agent 适配器：克劳德 (Claude)、吉米 (Kimi)、迪普斯克 (DeepSeek)、钱文 (Qwen)，均通过 CLI 抽象层接入。
- 基于 Worker Threads 为每个模型分配独立运行线程。
- 线程隔离，互不干扰。
- 心跳检测：定时探测各 Agent 状态，状态写入对应 `logs/agents/{agent_name}.log`。

#### 3.1.2 复杂任务标准化协作流程（对应需求 1.2）

实现 9 步协作流程状态机：

并行调研 → 结果汇总 → 交叉讨论 → 意见汇总 → 方案评定 → 执行确认 → 任务执行 → 结果评价 → 动态修正 → 完成。

- 执行确认支持人工确认 / 自动确认双模式。
- 动态修正环节支持最多 3 轮迭代。
- 评测不通过 → 自动退回执行 Agent 重新开发，3 轮后挂起。
- 每轮迭代的产出存入 `tasks/{task_id}/snapshots/`。
- 评审意见存入 `tasks/{task_id}/reviews/`，每个 Agent 独立文件。
- 超时检测：全环节设置超时阈值，防止流程死锁。

#### 3.1.3 评审强制规则与兜底（对应需求 四）

- 评审任务必须至少 2 个不同模型 Agent 参与。
- 不满足时：等待 30 秒重试 → 提示用户确认是否放宽 → 仍不满足则挂起。
- 健康校验：任务分发前检查各 Agent 心跳状态。

#### 3.1.4 任务智能分配引擎（对应需求 五）

- 任务标准化画像：解析用户需求自动生成任务画像（名称、难度、所需能力、类型、优先级）。
- Agent 能力标签匹配算法，匹配度排序，优先分配高匹配度 Agent 执行，次优 Agent 评审。

#### 3.1.5 全维度状态管控（对应需求 六）

任务状态核心字段存储于 `tasks/{task_id}/task.json`：

```json
{
  "task_id": "uuid",
  "name": "任务名称",
  "status": "executing | completed | suspended | failed",
  "difficulty": "simple | medium | complex | hard",
  "priority": "urgent | high | medium | low",
  "complex_flag": true,
  "required_capabilities": ["code", "review"],
  "task_type": "development | review | writing | design | verification",
  "executor_agent": "克劳德",
  "reviewer_agents": ["吉米", "迪普斯克"],
  "guardian_agent": null,
  "progress": 0.6,
  "revision_round": 1,
  "max_revision_rounds": 3,
  "suspend_reason": null,
  "handover_from": null,
  "handover_to": null,
  "created_at": "2026-08-11T10:00:00Z",
  "finished_at": null
}
```

Agent 状态存储于 `config/agents.yaml` 引用 + 运行时状态文件。

任务执行权交接：记录在不同 Agent 之间流转的完整轨迹。

#### 3.1.6 跨模型代码评审机制（对应需求 八）

- 基础硬性校验：代码必须通过测试用例。
- 跨模型交叉评审：至少 2 个不同模型独立 Review。
- 业务目标校验：判断功能实现是否匹配任务需求。
- 评审意见冲突裁决：同维度少数服从多数，票数均等时由克劳德辅助裁决。
- 评审不通过 → 退回重试 3 轮 → 挂起人工审核。

### 3.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| Multi-Agent Runtime | Worker Threads + 四大模型适配器 | 4 个 Agent 并行运行，线程隔离 |
| Workflow Engine | 有限状态机 | 9 步协作流程完整编排 |
| Task Matcher | 能力标签匹配算法 | 任务画像生成 + Agent 智能匹配 |
| Review Engine | 多模型并行 Review | 测试校验 → 跨模型评审 → 目标校验 → 冲突裁决 |
| File State Store | 结构化 JSON 文件 | 任务 / Agent / 交接权完整存储 |
| File Event Bus | 文件系统事件（增强版） | 多模块间异步事件通知可靠 |

### 3.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 多 Agent 并行 | 第 3 周末 | 3 周 | 四大模型适配器、线程隔离、并行调度 |
| M2: 协作流程 | 第 5 周末 | 2 周 | 9 步流程引擎、状态文件存储 |
| M3: 智能调度 + 评审 | 第 7 周末 | 2 周 | 任务画像匹配、跨模型评审、冲突裁决 |
| M4: 验收 + 修复 | 第 8 周末 | 1 周 | 联调测试、Bug 修复、功能验收 |

### 3.4 第二期量化验收指标

- 4 个 Agent 并行运行无线程冲突。
- 9 步协作流程完整走通，状态文件记录完整。
- 评审强制规则生效：不满足 2 个差异化 Agent 时进入降级流程。
- 评审冲突互斥时正确裁决。
- 测试用例不通过自动退回重试 3 轮。
- 任务状态文件、快照文件、评审意见文件格式规范、内容完整。

---

## 四、第三期：智能闭环期（预计 4-6 周）

核心目标：愿景守护机制、调度策略优化、任务效能分析报表。

### 4.1 核心功能范围

#### 4.1.1 多 Agent 愿景守护机制（对应需求 九）

- 闲置 Agent 自动切换为愿景守护模式。
- 愿景守护 Agent 全程旁观任务流程，读取 `tasks/{task_id}/` 下各阶段产出。
- 偏离检测：基于任务初始画像持续比对当前产出方向，识别偏离信号。
- 单任务最多输出 5 条纠偏建议，存入 `tasks/{task_id}/guardian_notes.md`。
- 评审意见质量校验：守护 Agent 同步读取评审内容，识别无效评审建议。

#### 4.1.2 多维 Agent 综合评分

基于历史任务数据，每日自动计算各 Agent 综合评分：

- 任务完成时效 30%。
- 代码评审通过率 40%。
- 愿景守护偏离次数 30%（反向指标）。

评分作为后续调度优先级排序的参考依据。

#### 4.1.3 调度策略优化

- 基于综合评分 + 繁忙度 + 负载均衡多因素调度。
- 紧急任务优先分配。
- 调度策略参数通过配置文件可调。

#### 4.1.4 任务效能分析报表

基于本地任务历史数据生成统计分析：

- 任务完成率、各 Agent 贡献度、协作效率趋势。
- 各模型在不同任务类型下的表现对比。
- 协作流程瓶颈分析：识别耗时最长、失败率最高的环节。
- 特性清单完成度统计：读取 `features/` 目录汇总各特性开发状态。
- 报表以 Markdown 文件输出至项目根目录供查看。

### 4.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| Vision Guardian | 事件驱动 + 语义相似度比对 | 闲置 Agent 自动守护，偏离识别准确率达标 |
| Multi-Dim Scorer | 加权多维评分 | 时效 + 通过率 + 偏离次数三维评分 |
| Smart Scheduler | 综合评分 + 多因素均衡 | 评分高 Agent 优先调度 |
| Analytics Reporter | 本地数据聚合 + Markdown 报表 | 效能 / 模型对比 / 瓶颈分析 / 特性进度四组报表 |

### 4.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 愿景守护 | 第 2 周末 | 2 周 | 守护 Agent + 偏离检测 + 评审校验 |
| M2: 评分 + 调度优化 | 第 4 周末 | 2 周 | 多维评分模型 + 调度策略优化 |
| M3: 效能报表 | 第 5 周末 | 1 周 | 四组报表输出 |
| M4: 验收 + 修复 | 第 6 周末 | 1 周 | 全量回归测试、Bug 修复、终验 |

### 4.4 第三期量化验收指标

- 偏离识别准确率 ≥ 85%。
- 评审意见质量问题识别率达标。
- 多维评分每日自动计算。
- 效能报表格式规范、数据准确。
- 特性清单完成度统计与实际情况一致。

---

## 五、核心数据文件定义

### 5.1 Agent 角色配置（config/agents.yaml）

```yaml
agents:
  - name: "克劳德"
    model_cli: "claude"
    capabilities: ["logic", "complex_code", "architecture", "text_refinement"]
    strengths: ["逻辑严谨", "代码规范", "长文本处理"]
    weaknesses: ["简单任务执行效率略低"]
    scenarios: ["complex_development", "architecture_design", "code_review"]
    default: true  # 默认承接 Agent

  - name: "吉米"
    model_cli: "kimi"
    capabilities: ["long_text", "information_extraction", "documentation"]
    strengths: ["大容量文本处理", "信息提炼精准"]
    weaknesses: ["复杂算法优化能力较弱"]
    scenarios: ["document_analysis", "content_generation"]

  - name: "迪普斯克"
    model_cli: "deepseek"
    capabilities: ["algorithm", "performance", "bug_detection", "deep_tech"]
    strengths: ["技术深度足", "专业性高", "纠错能力突出"]
    weaknesses: ["通用文本创作适配性一般"]
    scenarios: ["algorithm_optimization", "performance_tuning", "vulnerability_detection"]

  - name: "钱文"
    model_cli: "qwen"
    capabilities: ["lightweight_dev", "fast_iteration", "scenario_fitting", "chinese_optimization"]
    strengths: ["响应速度快", "场景适配广", "兼容性强"]
    weaknesses: ["超复杂架构设计能力有限"]
    scenarios: ["rapid_development", "localization", "simple_tasks"]
```

### 5.2 Git 仓库配置（config/git.yaml）

```yaml
git:
  default_repo_url: "https://github.com/user/default-repo.git"
  default_branch: "main"
  worktree_root: "./workspace"
  auto_cleanup_days: 7
```

### 5.3 任务默认参数（config/task_defaults.yaml）

```yaml
task_defaults:
  timeout_seconds: 600
  max_revision_rounds: 3
  auto_confirm: false
  guardian_enabled: false
```

### 5.4 特性清单文件格式（features/feat_xxx.md）

```markdown
# 特性名称

- **描述**：简要描述该特性的功能与目标
- **状态**：规划中 | 开发中 | 已完成 | 已暂停
- **开发进度描述**：当前已完成内容与剩余工作（每完成一个阶段更新此处）
- **关联任务**：task_id_xxx
- **创建时间**：2026-08-11
- **最后更新**：2026-08-11
```

### 5.5 特性清单索引（features/INDEX.md）

```markdown
# 特性清单索引

| 编号 | 特性名称 | 状态 | 关联任务 | 最后更新 |
|------|----------|------|----------|----------|
| feat_001 | 单Agent任务闭环 | 已完成 | task_001 | 2026-08-11 |
| feat_002 | 多Agent并行协作 | 开发中 | task_002 | 2026-08-11 |
| feat_003 | 愿景守护机制 | 规划中 | — | — |
```

---

## 六、关键风险与应对策略

| 风险项 | 影响 | 概率 | 应对策略 |
|--------|------|------|----------|
| 多模型 CLI 兼容性差异 | 部分模型无法正常调用 | 高 | CLI 抽象层统一接口，每接入新模型做兼容性回归测试 |
| 第三方模型服务故障 | 接口宕机、超时 | 高 | 各模型独立健康评分，连续失败自动切换备选 Agent，自动探测恢复 |
| 并行 Agent 资源竞争 | CPU / 内存过载 | 中 | Agent 资源上限可配置，支持降级串行 |
| 协作流程死锁 | 某环节 Agent 超时卡死 | 中 | 全环节超时检测，超时自动挂起或跳过 |
| 评审质量不稳定 | 不同模型评审深度差异大 | 中 | 冲突裁决机制兜底，三期愿景守护校验评审质量 |
| 本地文件并发冲突 | 多线程同时写文件 | 低 | 文件锁 + 写入幂等设计，任务目录独立隔离 |
| Git worktree 冲突 | 并发操作冲突 | 低 | worktree 命名绑定 task_id，定时清理残留目录 |

---

## 七、需求覆盖率矩阵

| 需求条款 | 第一期 | 第二期 | 第三期 | 说明 |
|----------|--------|--------|--------|------|
| 1.1 默认承接 Agent & 分级执行 | 完整 | — | — | 一期克劳德 Agent + 默认模型判别复杂度 |
| 1.2 复杂任务 9 步协作流程 | — | 完整 | — | 二期实现 9 步流程编排 |
| 二 多 Agent 并行运行机制 | — | 完整 | — | 二期四大模型适配器 + 线程隔离 |
| 三 Agent 角色预制体系 | 部分 | 完整 | — | 一期克劳德角色配置，二期四角色全量 |
| 四 Agent 健康检查与兜底 | — | 完整 | — | 二期心跳 + 评审强制规则 |
| 五 任务智能分配 | — | 完整 | 增强 | 二期匹配引擎，三期多维评分优化 |
| 六 全维度状态管控 | 基础 | 完整 | — | 一期核心字段，二期完整三维状态 |
| 七 代码隔离 Worktree | 完整 | — | — | 一期完整实现 |
| 八 跨模型代码评审 | — | 完整 | — | 二期双重校验 + 冲突裁决 |
| 九 愿景守护机制 | — | — | 完整 | 三期完整实现 |
| 十（新增）CLI 抽象层 | 完整 | 增强 | — | 一期接口定义 + 克劳德适配器，二期其余三个适配器 |
| 十一（新增）特性清单 | 完整 | 增强 | 增强 | 一期模板 + 索引，三期纳入效能报表 |
| 十二（新增）本地文件存储 | 完整 | — | — | 一期全部使用文件存储，贯穿全程 |
| 十三（新增）Git 仓库配置 | 完整 | — | — | 一期默认配置 + 用户可修改配置文件 |

---

## 八、交付与验收体系

### 8.1 测试门禁

单元测试（覆盖率 ≥ 70%）→ 集成测试 → 功能测试（需求点验收）→ UAT。

### 8.2 交付物清单

- 应用代码（含所有脚本）。
- 配置文件模板（agents.yaml / git.yaml / task_defaults.yaml）。
- 使用说明文档：环境要求、启动步骤、配置修改指引、任务操作说明。
- 特性清单索引与文件（随开发过程持续更新）。
- API 接口文档（CLI 抽象层接口定义、扩展适配器开发指南）。

### 8.3 各阶段量化验收指标

| 阶段 | 核心指标 | 阈值 |
|------|----------|------|
| 第一期 | 简单任务端到端执行成功 | 100% |
| | CLI 抽象层屏蔽底层差异 | 克劳德版本升级无需改业务代码 |
| | Worktree 创建成功率 | 100% |
| | 配置文件格式校验通过 | 无格式错误 |
| | 特性清单文件模板生成 | 字段齐全 |
| 第二期 | 4 Agent 并行无冲突 | 持续运行无线程冲突 |
| | 9 步流程完好率 | 100% 状态可追溯 |
| | 评审强制规则生效 | 降级流程正确 |
| | 任务状态文件完整性 | 所有字段有值 |
| 第三期 | 偏离识别准确率 | ≥ 85% |
| | 评分模型计算正确 | 每日自动更新 |
| | 效能报表数据准确 | 与任务文件一致 |
| | 特性清单进度统计 | 与实际一致 |

---

*文档结束*以下是按你的要求更新后的 **V2.1 完整方案**（第一期增加 Web 交互界面）：

---

# 多智能体（Multi-Agent）协作平台三期开发方案设计 V2.1

**版本：V2.1**　　**日期：2026-08-11**　　**状态：定稿**

---

## 一、项目概述

本文档基于《多智能体（Multi-Agent）协作平台产品需求说明》，制定分三期交付的开发方案。平台定位为**本地化运行的多智能体协作工具**，基于本地配置文件驱动，以 Web 界面为主要交互入口，无需外部数据库、消息中间件、账户体系等重型基础设施，通过多模型智能体分工协作、相互评审、动态纠错、愿景守护，实现复杂开发及内容生产任务的标准化、高质量落地。

### 1.1 三期总体策略

**第一期（MVP 基础能力期，6-8 周）**：单 Agent 任务执行闭环 + 完整 Web 交互界面，本地文件配置驱动，任务复杂度由默认模型自动判别。

**第二期（协作增强期，6-8 周）**：多 Agent 并行协作、9 步标准化协作流程、跨模型交叉评审、状态管控、特性清单追踪。

**第三期（智能闭环期，4-6 周）**：愿景守护、调度策略优化、任务效能分析与报表。

### 1.2 技术架构总览

平台采用本地四层架构，前端以本地 Web 服务形式提供：

**接入层**——Web 交互界面（本地 HTTP 服务）+ CLI 命令行入口。前端为 React SPA，通过本地 REST API 与后端通信，支持对话式交互、任务管理、Agent 状态查看。前端内嵌埋点模块采集用户行为数据存储至本地日志文件。

**调度层**——Task Orchestrator + Agent Scheduler，任务解析 → 画像生成 → 流程编排 → Agent 匹配 → 状态管理。前端通过本地 API 调用，调度层全流程基于本地文件驱动。

**执行层**——Multi-Model CLI Runtime，通过统一 CLI 抽象适配层调用各模型厂商 CLI，线程隔离并行运行。CLI 抽象层对各模型厂商 CLI 的输入输出做统一标准化定义，项目运行时所有 Agent 均通过该抽象接口通信。

**状态层**——Local File State Store + File Event Bus，任务数据、Agent 状态、协作流程信息、评审意见、快照等全部使用本地结构化文件（JSON / Markdown）存储。Event Bus 基于文件系统实现组件间事件通知，通过 WebSocket 将状态变更实时推送至前端。

### 1.3 本地文件存储规范

```
project-root/
├── config/
│   ├── agents.yaml          # Agent 角色配置（启动时加载，运行期只读）
│   ├── git.yaml              # Git 仓库配置（默认仓库 + 支持用户修改）
│   ├── users.yaml            # 简易用户配置（用户标识 + 角色，用于界面权限控制）
│   └── task_defaults.yaml    # 任务默认参数（超时、重试次数等）
├── tasks/
│   ├── index.json            # 任务索引（所有任务列表与当前状态）
│   └── {task_id}/            # 每个任务独立目录
│       ├── task.json         # 任务详细信息与进度
│       ├── conversation.md   # 对话记录（用户输入 + Agent 回复）
│       ├── snapshots/        # 各轮迭代快照
│       ├── reviews/          # 评审意见文件
│       └── outputs/          # 任务产出文件（代码、方案等）
├── features/                 # 特性清单（每个特性一个 .md 文件）
│   ├── INDEX.md
│   └── feat_xxx.md
├── logs/
│   ├── agents/               # Agent 运行日志
│   ├── events/               # Event Bus 事件日志
│   └── analytics/            # 前端埋点数据（页面加载、接口报错、任务取消等）
├── frontend/                 # 前端构建产物（SPA 静态文件）
└── workspace/                # Git worktree 工作区
```

### 1.4 CLI 抽象层设计

各模型厂商提供的 CLI 工具在输入输出格式、参数风格、错误码定义上存在差异。项目在接入每个模型 CLI 前，先定义统一的 CLI 抽象接口：

**统一输入格式**：

```json
{
  "task_id": "uuid",
  "role": "executor | reviewer | guardian",
  "context": "任务上下文与背景",
  "instruction": "具体执行指令",
  "input_files": ["file_paths"],
  "max_tokens": 4096
}
```

**统一输出格式**：

```json
{
  "task_id": "uuid",
  "status": "success | failed | timeout",
  "content": "产出文本内容",
  "output_files": ["file_paths"],
  "error": { "code": "xxx", "message": "xxx" },
  "tokens_used": 1234,
  "duration_ms": 5000
}
```

每个模型 CLI 对应一个适配器实现，负责将统一输入翻译为对应 CLI 的原生参数格式，并将原生输出标准化为统一输出格式。运行时所有 Agent 调度逻辑仅依赖抽象接口，不直接调用各厂商原始 CLI。

### 1.5 简易用户配置（config/users.yaml）

为支持前端界面中「仅创建人 / 管理员可操作批量删除」等权限控制，使用本地配置文件做简易用户识别。启动时加载，运行期不做复杂鉴权，仅做操作来源区分。

```yaml
users:
  - id: "user_default"
    name: "默认用户"
    role: "admin"
```

前端在请求 API 时携带 `user_id` 参数，后端校验任务归属与操作权限。该机制非完整账户体系，仅用于本地多人共用时的任务隔离与操作保护。

---

## 二、第一期：MVP 基础能力期（预计 6-8 周）

核心目标：搭建项目骨架，实现单 Agent 任务闭环 + 完整 Web 交互界面，建立本地配置驱动、CLI 抽象层、Git worktree 隔离、特性清单追踪机制。

### 2.1 核心功能范围

#### 2.1.1 本地配置启动机制

- 项目启动时读取 `config/agents.yaml` 加载 Agent 角色配置，运行期间角色数量与配置固定。
- 读取 `config/git.yaml` 加载 Git 仓库配置，内置一个默认仓库地址，支持用户编辑配置文件变更仓库。
- 读取 `config/users.yaml` 加载用户配置，用于前端界面操作权限区分。
- 读取 `config/task_defaults.yaml` 加载任务默认参数。

#### 2.1.2 CLI 抽象层实现

- 定义统一 CLI 输入输出标准化接口。
- 一期仅实现克劳德（Claude Code CLI）适配器，其余模型适配器二期开发。

#### 2.1.3 默认承接 Agent（对应需求 1.1）

- 实现克劳德 Agent 作为平台默认承接智能体。
- 基于 CLI 抽象层与 Agent 建立通信。

#### 2.1.4 任务复杂度自动判别（对应需求 1.1）

- 任务复杂度判断完全交给默认模型（克劳德）自行判别：接收用户输入后，克劳德自动评估任务为简单或复杂，评估结果写入 `task.json`。
- 简单任务：克劳德独立执行，直接输出结果。
- 复杂任务：标记 `complex_flag: true`，界面提示「完整多 Agent 协作将于二期上线」，便于二期迁移。

#### 2.1.5 单 Agent 任务全流程闭环

- 任务解析 → 复杂度判别 → Agent 执行 → 结果返回至本地输出目录。
- 任务状态机：执行中 → 执行结束 / 执行失败。
- 对话记录完整存入 `tasks/{task_id}/conversation.md`。
- 任务数据存入 `tasks/{task_id}/task.json`，索引更新至 `tasks/index.json`。

#### 2.1.6 代码开发与 Git 隔离（对应需求 七）

- 基于 `config/git.yaml` 中配置的默认仓库，创建独立 worktree 进行代码开发。
- worktree 命名绑定 `task_id`，目录位于 `workspace/{task_id}/`。
- 生命周期管理：创建、清理。定时清理任务状态为失败 / 终止且 7 天以上的 worktree。

#### 2.1.7 Web 交互界面

平台提供完整的本地 Web 前端界面，作为用户与多智能体协作平台的主要交互入口。

**启动方式**：项目启动后自动在本地端口（默认 `localhost:3000`）启动 HTTP 服务，浏览器访问即可使用。

**对话式交互模块**：

- 左侧对话面板，支持用户以自然语言输入任务需求，与默认承接 Agent 进行多轮对话。
- 对话历史实时渲染，支持 Markdown 格式展示（代码高亮、表格、列表）。
- 任务执行过程中，Agent 的思考过程与阶段性产出流式展示在对话区。
- 对话记录自动存入 `tasks/{task_id}/conversation.md`，任务完成后可回看完整对话历史。
- 新建任务 / 继续已有任务双入口：新建任务自动分配 `task_id`，已有任务从对话历史处继续。

**任务列表模块**：

- 右侧任务列表面板，以列表形式展示所有任务。
- 每条任务展示字段：任务名称、状态（执行中 / 已完成 / 失败 / 挂起）、进度百分比、复杂度标记（简单 / 复杂）、优先级（紧急 / 高 / 中 / 低）、执行 Agent 名称、关联 Git 仓库名、创建时间。
- 支持按状态筛选（全部 / 执行中 / 已完成 / 失败 / 挂起）。
- 支持按创建时间排序（最新 / 最早）。
- 点击任务条目展开详情面板：查看完整任务信息、对话历史、输出文件列表。

**任务批量操作模块**：

- 任务列表每条左侧有复选框，支持多选。
- 选中后显示批量操作工具栏。
- 批量导出：将所选任务的快照（task.json + conversation.md + outputs/）打包为 zip 文件下载至本地。
- 批量删除：删除所选任务目录及其关联的 worktree，仅允许删除「终止」或「失败」状态的任务，执行中的任务不可删除。操作前弹出二次确认。
- 权限控制：基于 `config/users.yaml` 中的用户配置，仅任务创建者本人或 `role: admin` 的用户可执行批量删除操作。非授权用户操作按钮置灰。

**Agent 状态面板模块**：

- 界面顶部或侧边栏固定 Agent 状态面板。
- 展示各 Agent 角色名称、绑定模型、当前在线状态（在线 / 离线）、运行状态（闲置 / 忙碌中）、当前承接任务、最后心跳时间。
- 在线状态通过心跳检测实时更新（默认每 5 秒刷新），离线 Agent 以灰色标识。
- 一期仅展示克劳德 Agent 状态，二期四角色全部展示。

**前端埋点模块**：

- 前端嵌入行为采集模块，自动记录以下指标数据并写入 `logs/analytics/` 目录：

| 指标 | 采集方式 | 存储格式 |
|------|----------|----------|
| 页面加载耗时 | Performance API 记录首屏渲染时间 | `page_load.jsonl` |
| 接口报错频次 | 拦截所有 API 4xx/5xx 响应 | `api_errors.jsonl` |
| 任务取消率 | 统计创建任务数 vs 手动终止任务数 | `task_cancel.jsonl` |

- 每条记录包含 `timestamp`、`user_id`、指标类型、具体数值或错误信息。
- 数据仅供三期效能分析报表读取，一期不提供可视化。

**技术选型**：

- 前端框架：React + Tailwind CSS。
- 后端 API：Node.js（Express / Fastify）本地 HTTP 服务。
- 实时推送：WebSocket（Agent 状态变更、任务进度更新推送前端）。
- 构建产物存放于 `frontend/` 目录，与后端同进程启动。

#### 2.1.8 特性清单追踪机制

- `features/INDEX.md` 维护全部特性总索引。
- 每个特性文件包含：描述、状态（规划中 / 开发中 / 已完成 / 已暂停）、开发进度描述、关联任务、创建时间、最后更新。
- 启动特性开发时自动生成该文件，开发过程中按进度修改。

#### 2.1.9 File Event Bus

基于文件系统的简易事件总线：

- 各模块向 `logs/events/` 写入事件文件（`{timestamp}_{event_type}.json`）。
- 消费者模块通过文件变更监听读取事件。
- 事件类型：任务状态变更、Agent 状态变更。
- 一期实现单进程内模块间通知，不依赖外部 MQ。

### 2.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| 配置加载器 | 读取 YAML 本地文件 | 启动时正确加载 agents / git / users / task_defaults |
| CLI 抽象层 | 统一接口 + 克劳德适配器 | 输入输出标准化，底层差异已屏蔽 |
| Agent Runtime | CLI 抽象接口调用 | 单 Agent 进程启停正常 |
| Task Orchestrator | 状态机 + 本地文件存储 | 任务创建 → 执行 → 结束全链路可追踪 |
| Git Worktree Manager | Git CLI 封装 | worktree 创建 / 清理，定时回收 |
| File Event Bus | 文件系统事件写入 + 读取 | 模块间状态变更事件可达 |
| 特性清单系统 | Markdown 模板 + 索引 | 特性文件自动生成，进度实时更新 |
| Web 前端 | React + Tailwind CSS | 对话交互 + 任务列表 + 批量操作 + Agent 面板 + 埋点 |
| API 服务 | Node.js 本地 HTTP + WebSocket | 任务 CRUD + Agent 状态推送 + 文件导出 |

### 2.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 项目骨架 | 第 1 周末 | 1 周 | 目录结构、配置加载、CLI 抽象层接口定义、API 服务框架 |
| M2: 单 Agent 链路 | 第 3 周末 | 2 周 | 克劳德适配器、任务复杂度判别、简单任务闭环、对话记录存储 |
| M3: 前端界面 | 第 5 周末 | 2 周 | 对话交互界面、任务列表、Agent 状态面板、埋点模块 |
| M4: 工作区 + 批量操作 + 特性清单 | 第 7 周末 | 2 周 | Git worktree 隔离、批量导出/删除、特性清单系统、File Event Bus |
| M5: 验收 + 修复 | 第 8 周末 | 1 周 | 端到端联调、Bug 修复、功能验收 |

### 2.4 第一期量化验收指标

- 本地配置正确加载，启动无报错，Web 界面可通过浏览器正常访问。
- 克劳德 Agent 通过 CLI 抽象层正常通信。
- 任务复杂度自动判别正确，复杂任务标记不误判并在界面提示。
- 简单任务端到端执行成功，对话记录完整，结果可查看。
- 对话界面支持 Markdown 渲染，多轮对话历史可完整回看。
- 任务列表展示字段齐全（状态、进度、复杂度、优先级、Agent、仓库、时间）。
- 批量导出：多选任务正确打包为 zip 文件下载。
- 批量删除：权限校验生效（非创建人 / 非管理员禁止操作），二次确认弹窗正常。
- Agent 状态面板：心跳实时更新（5 秒刷新），离线 / 闲置 / 忙碌状态正确显示。
- 前端埋点数据正确写入 `logs/analytics/`，字段格式规范。
- Git worktree 创建 / 清理成功率 100%。
- 特性清单文件按模板自动生成，字段齐全。
- Event Bus 事件文件写入 + WebSocket 推送延迟 ≤ 500ms。

---

## 三、第二期：协作增强期（预计 6-8 周）

核心目标：引入多 Agent 并行协作、9 步标准化协作流程、跨模型交叉评审、全维度状态管控。前端界面同步升级。

### 3.1 核心功能范围

#### 3.1.1 多 Agent 并行运行机制（对应需求 二）

- 实现四大 Agent 适配器：克劳德 (Claude)、吉米 (Kimi)、迪普斯克 (DeepSeek)、钱文 (Qwen)，均通过 CLI 抽象层接入。
- 基于 Worker Threads 为每个模型分配独立运行线程，隔离互不干扰。
- 心跳检测：定时探测各 Agent 状态，状态写入对应 `logs/agents/{agent_name}.log`，前端 Agent 面板实时展示四角色状态。

#### 3.1.2 复杂任务标准化协作流程（对应需求 1.2）

实现 9 步协作流程状态机：

并行调研 → 结果汇总 → 交叉讨论 → 意见汇总 → 方案评定 → 执行确认 → 任务执行 → 结果评价 → 动态修正 → 完成。

- 执行确认支持人工确认 / 自动确认双模式，人工确认通过前端界面操作。
- 动态修正最多 3 轮迭代。
- 评测不通过 → 自动退回重试 3 轮后挂起。
- 每轮快照、评审意见均存入任务目录对应子文件夹。
- 全环节超时检测。
- 流程进度实时推送前端，可视化展示当前所处步骤。

#### 3.1.3 前端升级

- 对话界面支持多 Agent 对话分屏：并行调研阶段同时展示各 Agent 输出。
- 任务详情页新增「协作流程」子面板：以时间线 / 步骤图展示 9 步流程当前进度。
- 评审页面：展示各 Agent 评审意见对照、冲突标记高亮、裁决结果。

#### 3.1.4 评审强制规则与兜底（对应需求 四）

- 评审任务必须至少 2 个不同模型 Agent 参与。
- 不满足时分层降级：等待 30 秒重试 → 提示用户确认是否放宽 → 挂起。

#### 3.1.5 任务智能分配引擎（对应需求 五）

- 任务标准化画像自动生成。
- Agent 能力标签匹配算法，高匹配度 Agent 执行，次优 Agent 评审。

#### 3.1.6 全维度状态管控（对应需求 六）

- 任务状态存储于 `tasks/{task_id}/task.json`（完整字段见原需求）。
- Agent 状态存储于 `config/agents.yaml` 引用 + 运行时状态文件。
- 交接权流转轨迹完整记录。

#### 3.1.7 跨模型代码评审机制（对应需求 八）

- 测试用例硬性校验。
- 跨模型交叉评审（至少 2 个不同模型独立 Review）。
- 业务目标校验。
- 评审冲突裁决：少数服从多数，票数均等时克劳德辅助裁决。

### 3.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| Multi-Agent Runtime | Worker Threads + 四大模型适配器 | 4 个 Agent 并行运行，线程隔离 |
| Workflow Engine | 有限状态机 | 9 步协作流程完整编排，进度前端可视化 |
| Task Matcher | 能力标签匹配算法 | 任务画像生成 + Agent 智能匹配 |
| Review Engine | 多模型并行 Review | 测试校验 → 跨模型评审 → 目标校验 → 冲突裁决 |
| File State Store | 结构化 JSON 文件 | 任务 / Agent / 交接权完整存储 |
| File Event Bus | 文件系统事件（增强版） | 多模块间异步通知 + WebSocket 实时推送 |
| 前端升级 | React 对话分屏 + 流程可视化 + 评审对照 | 协作流程进度、评审冲突高亮、裁决依据展示 |

### 3.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 多 Agent 并行 | 第 3 周末 | 3 周 | 四大模型适配器、线程隔离、并行调度 |
| M2: 协作流程 + 前端升级 | 第 5 周末 | 2 周 | 9 步流程引擎、协作流程可视化、对话分屏 |
| M3: 智能调度 + 评审 | 第 7 周末 | 2 周 | 任务画像匹配、跨模型评审、冲突裁决 |
| M4: 验收 + 修复 | 第 8 周末 | 1 周 | 联调测试、Bug 修复、功能验收 |

### 3.4 第二期量化验收指标

- 4 个 Agent 并行运行无线程冲突，前端面板均正常展示实时状态。
- 9 步协作流程完整走通，流程可视化步骤图与实际进度一致。
- 评审强制规则生效：不满足 2 个差异化 Agent 时进入降级流程。
- 评审冲突互斥时正确裁决，前端冲突标记高亮。
- 测试用例不通过自动退回重试 3 轮。
- 任务状态文件、快照文件、评审意见文件格式规范、内容完整。

---

## 四、第三期：智能闭环期（预计 4-6 周）

核心目标：愿景守护机制、调度策略优化、任务效能分析报表、运维看板。

### 4.1 核心功能范围

#### 4.1.1 多 Agent 愿景守护机制（对应需求 九）

- 闲置 Agent 自动切换为愿景守护模式。
- 全程旁观任务流程，读取各阶段产出。
- 偏离检测引擎，识别产出偏离信号。
- 单任务最多输出 5 条纠偏建议。
- 评审意见质量校验：守护 Agent 同步校验评审内容质量。

#### 4.1.2 多维 Agent 综合评分

- 基于历史任务数据每日自动计算评分：任务完成时效 30% + 代码评审通过率 40% + 愿景守护偏离次数 30%。
- 评分作为后续调度优先级参考。

#### 4.1.3 调度策略优化

- 基于综合评分 + 繁忙度 + 负载均衡 + 优先级多因素调度。

#### 4.1.4 运维看板与效能报表

**运维看板（前端新增页面）**：

- Agent 性能指标卡片：各 Agent 任务处理量、成功率、平均响应时间。
- 任务全景：总任务数 / 成功 / 失败 / 挂起分布饼图。
- 协作效率趋势折线图：近 30 天任务完成数、平均耗时趋势。
- 用户行为指标卡片（读取一期埋点数据 `logs/analytics/`）：页面加载耗时 P50 / P95、接口报错频次 Top 5、任务取消率。
- 特性清单完成度进度条：读取 `features/INDEX.md` 统计各状态特性数量与占比。
- 所有图表基于 Chart.js 实现，数据源为本地文件。

**效能报表（Markdown 输出）**：

- 任务完成率、各 Agent 贡献度、协作效率趋势。
- 各模型在不同任务类型下的表现对比。
- 协作流程瓶颈分析：耗时最长、失败率最高的环节。
- 特性清单完成度统计。

### 4.2 技术交付物

| 模块 | 方案 | 交付标准 |
|------|------|----------|
| Vision Guardian | 事件驱动 + 语义相似度比对 | 闲置 Agent 自动守护，偏离识别准确率达标 |
| Multi-Dim Scorer | 加权多维评分 | 时效 + 通过率 + 偏离次数三维评分 |
| Smart Scheduler | 综合评分 + 多因素均衡 | 评分高 Agent 优先调度 |
| Dashboard | React + Chart.js + 本地文件数据源 | Agent 指标 / 任务全景 / 协作趋势 / 用户体验 / 特性进度五组看板 |
| Analytics Reporter | 本地数据聚合 + Markdown 报表 | 效能 / 模型对比 / 瓶颈分析 / 特性进度四组报表 |

### 4.3 里程碑与排期

| 里程碑 | 时间节点 | 周期 | 交付物 |
|--------|----------|------|--------|
| M1: 愿景守护 | 第 2 周末 | 2 周 | 守护 Agent + 偏离检测 + 评审校验 |
| M2: 评分 + 调度优化 | 第 4 周末 | 2 周 | 多维评分模型 + 调度策略优化 |
| M3: 看板 + 报表 | 第 5 周末 | 1 周 | 运维看板五组图表 + 四组 Markdown 报表 |
| M4: 验收 + 修复 | 第 6 周末 | 1 周 | 全量回归测试、Bug 修复、终验 |

### 4.4 第三期量化验收指标

- 偏离识别准确率 ≥ 85%。
- 多维评分每日自动计算。
- 运维看板五组图表数据准确，与本地文件一致。
- 前端埋点数据在看板中正确可视化。
- 效能报表格式规范、数据准确。
- 特性清单进度统计与实际情况一致。

---

## 五、核心数据文件定义

### 5.1 Agent 角色配置（config/agents.yaml）

```yaml
agents:
  - name: "克劳德"
    model_cli: "claude"
    capabilities: ["logic", "complex_code", "architecture", "text_refinement"]
    strengths: ["逻辑严谨", "代码规范", "长文本处理"]
    weaknesses: ["简单任务执行效率略低"]
    scenarios: ["complex_development", "architecture_design", "code_review"]
    default: true

  - name: "吉米"
    model_cli: "kimi"
    capabilities: ["long_text", "information_extraction", "documentation"]
    strengths: ["大容量文本处理", "信息提炼精准"]
    weaknesses: ["复杂算法优化能力较弱"]
    scenarios: ["document_analysis", "content_generation"]

  - name: "迪普斯克"
    model_cli: "deepseek"
    capabilities: ["algorithm", "performance", "bug_detection", "deep_tech"]
    strengths: ["技术深度足", "专业性高", "纠错能力突出"]
    weaknesses: ["通用文本创作适配性一般"]
    scenarios: ["algorithm_optimization", "performance_tuning", "vulnerability_detection"]

  - name: "钱文"
    model_cli: "qwen"
    capabilities: ["lightweight_dev", "fast_iteration", "scenario_fitting", "chinese_optimization"]
    strengths: ["响应速度快", "场景适配广", "兼容性强"]
    weaknesses: ["超复杂架构设计能力有限"]
    scenarios: ["rapid_development", "localization", "simple_tasks"]
```

### 5.2 Git 仓库配置（config/git.yaml）

```yaml
git:
  default_repo_url: "https://github.com/user/default-repo.git"
  default_branch: "main"
  worktree_root: "./workspace"
  auto_cleanup_days: 7
```

### 5.3 简易用户配置（config/users.yaml）

```yaml
users:
  - id: "user_default"
    name: "默认用户"
    role: "admin"
```

### 5.4 任务默认参数（config/task_defaults.yaml）

```yaml
task_defaults:
  timeout_seconds: 600
  max_revision_rounds: 3
  auto_confirm: false
  guardian_enabled: false
```

### 5.5 特性清单文件格式（features/feat_xxx.md）

```markdown
# 特性名称

- **描述**：简要描述该特性的功能与目标
- **状态**：规划中 | 开发中 | 已完成 | 已暂停
- **开发进度描述**：当前已完成内容与剩余工作
- **关联任务**：task_id_xxx
- **创建时间**：2026-08-11
- **最后更新**：2026-08-11
```

### 5.6 特性清单索引（features/INDEX.md）

```markdown
# 特性清单索引

| 编号 | 特性名称 | 状态 | 关联任务 | 最后更新 |
|------|----------|------|----------|----------|
| feat_001 | Web交互界面 | 开发中 | task_001 | 2026-08-11 |
| feat_002 | 单Agent任务闭环 | 已完成 | task_001 | 2026-08-11 |
| feat_003 | 多Agent并行协作 | 规划中 | — | — |
```

---

## 六、关键风险与应对策略

| 风险项 | 影响 | 概率 | 应对策略 |
|--------|------|------|----------|
| 多模型 CLI 兼容性差异 | 部分模型无法正常调用 | 高 | CLI 抽象层统一接口，每接入新模型做兼容性回归测试 |
| 第三方模型服务故障 | 接口宕机、超时 | 高 | 各模型独立健康评分，连续失败自动切换备选 Agent |
| 并行 Agent 资源竞争 | CPU / 内存过载 | 中 | Agent 资源上限可配置，支持降级串行 |
| 协作流程死锁 | 某环节 Agent 超时卡死 | 中 | 全环节超时检测，超时自动挂起或跳过 |
| 评审质量不稳定 | 不同模型评审深度差异大 | 中 | 冲突裁决机制兜底，三期愿景守护校验评审质量 |
| 本地文件并发冲突 | 多线程同时写文件 | 低 | 文件锁 + 写入幂等设计，任务目录独立隔离 |
| Git worktree 冲突 | 并发操作冲突 | 低 | worktree 命名绑定 task_id，定时清理残留 |
| 前端性能瓶颈 | 任务量增大后列表渲染卡顿 | 中 | 虚拟列表优化，分页加载，任务索引文件快速检索 |

---

## 七、需求覆盖率矩阵

| 需求条款 | 第一期 | 第二期 | 第三期 | 说明 |
|----------|--------|--------|--------|------|
| 1.1 默认承接 Agent & 分级执行 | 完整 | — | — | 一期克劳德 Agent + 默认模型判别复杂度 |
| 1.2 复杂任务 9 步协作流程 | — | 完整 | — | 二期实现 9 步流程编排 + 前端可视化 |
| 二 多 Agent 并行运行机制 | — | 完整 | — | 二期四大模型适配器 + 线程隔离 |
| 三 Agent 角色预制体系 | 部分 | 完整 | — | 一期克劳德，二期四角色全量 |
| 四 Agent 健康检查与兜底 | — | 完整 | — | 二期心跳 + 评审强制规则 |
| 五 任务智能分配 | — | 完整 | 增强 | 二期匹配引擎，三期多维评分 |
| 六 全维度状态管控 | 基础 | 完整 | — | 一期核心字段，二期完整三维状态 |
| 七 代码隔离 Worktree | 完整 | — | — | 一期完整实现 |
| 八 跨模型代码评审 | — | 完整 | — | 二期双重校验 + 冲突裁决 |
| 九 愿景守护机制 | — | — | 完整 | 三期完整实现 |
| 十（新增）CLI 抽象层 | 完整 | 增强 | — | 一期接口定义 + 克劳德适配器，二期其余三个 |
| 十一（新增）特性清单 | 完整 | 增强 | 增强 | 一期模板 + 索引，三期纳入看板统计 |
| 十二（新增）本地文件存储 | 完整 | — | — | 全程使用本地文件存储 |
| 十三（新增）Git 仓库配置 | 完整 | — | — | 一期默认配置 + 用户可修改 |
| 十四（新增）Web 交互界面 | 完整 | 增强 | 增强 | 一期对话/列表/面板/批量操作/埋点，二期协作可视化，三期运维看板 |

---

## 八、交付与验收体系

### 8.1 测试门禁

单元测试（覆盖率 ≥ 70%）→ 集成测试 → 功能测试（需求点验收）→ UAT。

### 8.2 交付物清单

- 应用代码（含后端 + 前端构建产物）。
- 配置文件模板（agents.yaml / git.yaml / users.yaml / task_defaults.yaml）。
- 使用说明文档：环境要求、启动步骤、配置修改指引、Web 界面操作说明、任务批量操作说明。
- 特性清单索引与文件（随开发过程持续更新）。
- API 接口文档（CLI 抽象层接口定义、扩展适配器开发指南、前端 API 接口文档）。

### 8.3 各阶段量化验收指标

| 阶段 | 核心指标 | 阈值 |
|------|----------|------|
| **第一期** | 简单任务端到端执行成功 | 100% |
| | CLI 抽象层屏蔽底层差异 | 克劳德版本升级无需改业务代码 |
| | Web 界面可正常访问与操作 | 对话 / 列表 / 面板 / 批量操作全功能可用 |
| | 任务列表展示字段齐全 | 状态、进度、复杂度、优先级、Agent、仓库、时间 |
| | 批量导出 zip 包正确 | 含 task.json + conversation.md + outputs |
| | 批量删除权限校验 | 非创建人 / 非管理员禁止 |
| | Agent 状态面板心跳更新 | ≤ 5 秒刷新，状态正确 |
| | 前端埋点数据写入 | 字段格式规范，路径正确 |
| | Worktree 创建成功率 | 100% |
| | 配置文件格式校验通过 | 无格式错误 |
| **第二期** | 4 Agent 并行无冲突 | 无线程冲突 |
| | 9 步流程完好率 | 100% 状态可追溯，前端可视化正确 |
| | 评审强制规则生效 | 降级流程正确 |
| | 任务状态文件完整性 | 所有字段有值 |
| **第三期** | 偏离识别准确率 | ≥ 85% |
| | 评分模型计算正确 | 每日自动更新 |
| | 运维看板数据准确 | 与本地源文件一致 |
| | 效能报表数据准确 | 与任务文件一致 |
| | 前端埋点数据看懂可视化 | 五组看板全部可用 |
| | 特性清单进度统计 | 与实际一致 |

---

*文档结束*