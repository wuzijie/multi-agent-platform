# 消息总线\+Redis黑板 多Agent协作生产级方案

# 一、整体架构设计（核心架构）

## 1\.1 架构定位

采用**事件驱动调度 \+ 内存级持久化黑板**的轻量化生产架构，规避传统数据库黑板延迟高、消息点对点耦合严重的问题，兼顾高性能、高可用、可追溯、可断点续跑。

核心分层：调度层、通信层、存储黑板层、Agent执行层

## 1\.2 核心组件分工

- **统一调度器（Scheduler）**：唯一状态决策者，负责任务拆分、DAG依赖解析、状态流转、重试、超时管控，所有状态变更由调度器统一写入，杜绝Agent私自改状态

- **消息总线（Redis Pub/Sub）**：承担事件通知、任务下发、结果回传，实现Agent完全解耦，异步驱动任务流转

- **Redis黑板（持久化）**：存储全量任务状态、子任务数据、Agent上下文、执行快照，开启RDB\+AOF持久化，替代传统数据库，实现毫秒级状态读写

- **多Agent集群**：无状态执行节点（规划Agent、执行Agent、审核Agent、汇总Agent等），仅负责消费事件、执行任务、返回结果，不持有任何状态

## 1\.3 核心架构流程（全事件\&回调驱动）

整套多Agent协作体系**摒弃定时轮询，所有动作、状态流转、任务调度均由事件触发、回调闭环驱动**。核心流程：用户请求触发初始化事件 → 调度器消费事件创建顶层任务 → 规划完成触发子任务拆分事件 → 总线下发任务事件 → Agent消费事件执行逻辑 → 执行结果触发回调事件 → 调度器消费回调事件更新黑板状态、触发下一轮调度事件 → 所有任务完结触发任务收尾事件，形成完整事件回调闭环。全程无主动轮询，所有流转被动响应事件，完全异步解耦。

# 二、核心数据模型定义

## 2\.1 全局唯一标识规范

- trace\_id：全局顶层任务唯一ID，贯穿所有子任务、消息、日志、快照

- sub\_task\_id：子任务唯一ID，trace\_id\+自增序号生成，全局唯一

- agent\_instance\_id：Agent实例唯一标识，用于定位故障节点、负载均衡

## 2\.2 Agent跨总线消息体标准（统一协议）

所有总线事件统一结构体，支持幂等、溯源、超时、重试管控，分为**任务下发、结果上报、异常上报、心跳通知**四类事件，通用基础结构如下：

```json
{
  "msg_meta": {
    "trace_id": "task_20260824_0001",
    "sub_task_id": "sub_001",
    "msg_id": "uuid唯一消息ID（幂等键）",
    "msg_type": "TASK_DISPATCH / TASK_SUCCESS / TASK_FAIL / AGENT_HEARTBEAT",
    "sender_agent": "scheduler / planner_agent_01",
    "receiver_agent": "code_agent_01 / all",
    "timestamp": 1787654321000,
    "timeout_ms": 300000,
    "retry_times": 0,
    "max_retry": 3
  },
  "task_context": {
    "task_input": {},
    "task_deps": ["sub_000"],
    "context_snapshot_key": "blackboard:snapshot:task_20260824_0001"
  },
  "task_result": {
    "output": {},
    "error_msg": "",
    "error_code": "",
    "cost_ms": 0
  }
}
```

字段说明：

- msg\_id：消息唯一主键，用于Redis幂等去重，避免重复执行

- context\_snapshot\_key：关联Redis黑板上下文快照，消息体轻量化，不存储大体积数据

- retry\_times/max\_retry：管控重试次数，避免无限重试

## 2\.3 Redis黑板存储模型（核心）

采用**Key分层设计**，所有数据持久化（RDB\+AOF开启），支持断点续跑、状态回溯、故障恢复，核心Key规范：

### 2\.3\.1 顶层任务Key

`blackboard:task:{trace_id}:main`（Hash结构）

```json
{
  "trace_id": "task_20260824_0001",
  "user_query": "用户原始请求",
  "overall_status": "PENDING/RUNNING/SUCCESS/FAILED/CANCELLED",
  "create_time": 1787654321000,
  "update_time": 1787654321000,
  "dag_info": "完整任务依赖拓扑",
  "final_result": "最终聚合结果",
  "fail_reason": ""
}
```

### 2\.3\.2 子任务Key

`blackboard:task:{trace_id}:sub:{sub_task_id}`（Hash结构，单子任务全量状态）

```json
{
  "sub_task_id": "sub_001",
  "trace_id": "task_20260824_0001",
  "agent_id": "code_agent_01",
  "status": "PENDING/RUNNING/SUCCESS/FAILED/RETRYING/TIMEOUT",
  "deps": ["sub_000"],
  "input": "子任务入参",
  "output": "子任务执行结果",
  "error_msg": "",
  "retry_count": 0,
  "max_retry": 3,
  "start_time": 1787654321000,
  "end_time": 0,
  "timeout_ms": 300000
}
```

### 2\.3\.3 上下文快照Key

`blackboard:snapshot:{trace_id}`（String/JSON结构）：存储任务全量中间上下文、对话历史、临时数据，避免消息体膨胀

### 2\.3\.4 幂等去重Key

`blackboard:idempotent:{msg_id}`（Set，过期时间=任务超时时间）：记录已消费消息，杜绝重复执行

### 2\.3\.5 僵尸任务检测Key

`blackboard:running:{sub_task_id}`（KV，带过期时间）：标记运行中任务，用于超时回收

## 2\.4 完整任务状态枚举

### 2\.4\.1 顶层任务状态

PENDING（待调度）→ RUNNING（执行中）→ SUCCESS（全部完成）/ FAILED（任务失败）/ CANCELLED（主动取消）

### 2\.4\.2 子任务状态（精细管控）

PENDING（待执行）→ RUNNING（执行中）→ SUCCESS（执行成功）/ RETRYING（重试中）/ TIMEOUT（执行超时）/ FAILED（最终失败）/ SKIPPED（跳过）

# 三、任务状态流转完整流程

## 3\.1 整体执行链路

全流程基于**事件触发\+回调闭环**驱动，无任何主动轮询操作，调度器与Agent仅被动消费总线事件执行对应逻辑，Agent无状态、无状态修改权限，从根源保证状态一致性与流转实时性。

1. **任务初始化**：用户提交请求，调度器生成唯一trace\_id，创建顶层任务（PENDING）写入Redis黑板

2. **任务规划拆分**：调度器调用规划Agent，拆分出DAG拓扑子任务，批量写入所有子任务（PENDING），顶层任务状态更新为RUNNING

3. **依赖就绪与Agent匹配**：调度器消费DAG依赖就绪事件，筛选出「依赖全部完成\+状态PENDING」的可执行子任务，触发**任务类型\-Agent能力画像匹配**逻辑，筛选最优执行Agent

4. **总线下发任务**：调度器组装标准消息体，通过Redis Pub/Sub推送对应Agent专属Topic，同时将子任务状态更新为RUNNING，写入running僵尸检测Key

5. **Agent执行任务**：Agent订阅Topic，消费消息，先校验幂等Key，无重复则读取Redis上下文快照执行任务

6. **结果回传总线**：Agent执行完成/异常，组装结果消息体推回总线结果Topic

7. **调度器更新状态**：调度器消费结果事件，更新子任务状态、结果、错误信息，清除running检测Key

8. **回调触发二次调度**：调度器消费Agent回调事件、更新黑板状态后，自动触发「任务状态变更事件」，重新解析DAG拓扑，自动推送后续可执行子任务事件，循环驱动流转

## 3\.2 核心流转规则

- 子任务必须满足「所有依赖子任务状态=SUCCESS」才可被调度执行

- RUNNING状态任务，超时未返回结果，调度器自动标记为TIMEOUT，触发重试机制

- 仅调度器拥有Redis黑板的**写权限**，Agent仅有读权限，所有状态变更、任务动作均由事件/回调触发，彻底避免状态错乱与主动轮询开销

# 四、消息总线通信机制 \& Agent能力匹配调度机制

## 4\.1 Topic主题规范（分类解耦）

整套系统所有交互均依托**事件发布\-订阅\+结果回调应答**机制，无直接调用、无主动轮询，所有业务动作、状态更新、分支流转均由事件触发。采用**定向订阅\+全局广播\+回调应答**混合模式，精准投递、闭环响应、互不干扰：

- `topic:task:dispatch:{agent_type}`：任务定向下发Topic（如code、review、summary Agent专属）

- `topic:task:callback`：所有Agent结果统一回传Topic，调度器统一消费

- `topic:system:heartbeat`：Agent心跳、故障通知广播Topic

## 4\.2 通信时序示例（标准串行协作）

全事件回调驱动时序（无轮询、纯异步闭环）：调度器发布任务下发事件 → 目标Agent订阅消费事件执行任务 → 任务完成/异常主动发布回调事件 → 调度器订阅消费回调事件、更新黑板状态 → 触发状态变更事件、驱动下一轮任务调度，所有环节均为事件触发、回调收尾。

## 4\.3 消息幂等保障机制

1. 每条消息携带唯一msg\_id

2. Agent消费前先查询Redis `blackboard:idempotent:{msg_id}`

3. 已存在则直接跳过，不存在则写入幂等Key，有效期匹配任务最大超时时间

4. 彻底解决Redis Pub/Sub重复投递、重连补发问题

## 4\.4 任务类型与Agent能力画像精准匹配调度（核心增强）

整套调度体系基于**任务类型标签 \+ Agent动态能力画像**实现精准任务分发，完全依托事件驱动触发匹配逻辑，无人工硬编码绑定。匹配失败、无最优匹配时，自动兜底分配至**空闲未被选中的在线Agent**，保障任务不阻塞、不丢失。

### 4\.4\.1 Agent能力画像模型（Redis动态存储）

调度器在Redis黑板维护所有在线Agent的能力画像，随Agent心跳事件动态更新，数据实时生效，Key规范：`blackboard:agent:profile:{agent_instance_id}`（Hash结构）

```json
{
  "agent_instance_id": "code_agent_01",
  "agent_type": "code_execute",
  "ability_tags": ["代码编写", "代码调试", "脚本运行", "语法校验"],
  "support_task_types": ["CODE_TASK", "DEBUG_TASK", "RUN_SCRIPT"],
  "load_score": 25,
  "status": "online/idle/busy",
  "last_heartbeat": 1787654321000
}
```

字段说明：

- ability\_tags：精细化能力标签，用于模糊匹配、泛化任务分发

- support\_task\_types：精准绑定可处理的任务类型枚举，用于严格匹配

- load\_score：负载分值（0\-100），数值越低越空闲，优先调度低负载Agent

- status：Agent实时状态（在线空闲/在线忙碌/离线），仅空闲Agent参与匹配

### 4\.4\.2 任务类型标签体系（与Agent能力一一映射）

所有子任务创建时，自动携带**task\_type**类型标签，写入子任务Redis数据，作为匹配唯一依据，核心任务类型枚举如下：

- PLAN\_TASK：任务规划拆解任务 → 匹配规划类Agent

- CODE\_TASK：代码编写执行任务 → 匹配代码执行Agent

- REVIEW\_TASK：内容审核校验任务 → 匹配评审Agent

- SUMMARY\_TASK：结果聚合汇总任务 → 匹配汇总Agent

- DEBUG\_TASK：问题排查调试任务 → 匹配调试专项Agent

### 4\.4\.3 事件驱动匹配流程（纯事件触发，无轮询）

任务匹配、分发全程由事件驱动，嵌入原有DAG就绪事件链路，流程闭环：

1. **触发时机**：调度器消费 `TASK_DEPEND_READY_EVENT` 依赖就绪事件，启动任务匹配逻辑

2. **精准匹配**：根据子任务task\_type，筛选出support\_task\_types包含该类型、状态为idle的低负载Agent

3. **最优分发**：优先选择负载最低、能力标签最匹配的Agent，绑定sub\_task\_id

4. **兜底匹配**：无精准匹配、专属Agent离线/繁忙时，自动筛选**所有在线空闲、未被选中的Agent**进行兜底分配

5. **事件下发**：匹配成功后，推送 `SUB_TASK_DISPATCH_EVENT` 至对应Agent专属Topic，完成任务分发

6. **负载更新**：Agent接收任务后，通过心跳事件上报负载变化，调度器实时更新Agent能力画像

### 4\.4\.4 匹配优先级规则（固定权重）

严格遵循「精准优先、兜底保底」原则，杜绝任务无Agent可执行：

1. 第一优先级：任务类型精准匹配 \+ Agent空闲 \+ 负载最低

2. 第二优先级：能力标签泛化匹配 \+ Agent空闲

3. 第三优先级：全量在线空闲Agent兜底分配（未被选中、无任务占用）

### 4\.4\.5 匹配失败兜底策略

- 无任何在线Agent匹配：触发 `AGENT_MATCH_EMPTY_EVENT` 事件，调度器暂存任务，等待Agent上线，不判定任务失败

- 专属Agent全部繁忙：自动轮转分配至空闲通用Agent，保证任务即时执行

- 所有Agent高负载：任务进入事件队列排队，按就绪顺序异步分发，避免系统拥堵

# 五、错误处理与容错机制（生产核心）

## 5\.1 故障场景分类与事件回调容错策略

### 5\.1\.1 Agent执行失败（业务异常、LLM输出异常）

- 触发条件：Agent返回error\_msg、异常堆栈、输出格式错误

- 处理逻辑：子任务状态改为RETRYING，retry\_count\+1，未达最大重试次数则重新入队调度，超过则标记为FAILED

- 重试策略：指数退避重试（1s、3s、5s、10s），避免瞬时拥堵

### 5\.1\.2 任务超时（僵尸任务）

- 触发条件：子任务标记RUNNING，超过timeout\_ms未返回结果，running Key过期

- 处理逻辑：依托**任务超时事件**触发容错，Redis running Key过期后自动推送超时事件，调度器消费事件标记状态为TIMEOUT，重新触发Agent能力匹配、分配新Agent重试

- 兜底机制：超时3次直接判定任务失败，终止重试，避免卡死

### 5\.1\.3 Agent实例崩溃/离线

- 心跳检测：Agent定时推送心跳事件至总线，调度器持续消费事件维护在线Agent列表，纯事件驱动感知节点状态

- 故障处理：总线检测到Agent离线，触发「Agent离线事件」，调度器消费事件后，将其名下所有RUNNING子任务回收，**重新执行能力匹配\+兜底分配**，推送任务事件至在线空闲Agent

### 5\.1\.4 消息丢失、总线断连

- 断连恢复后，Redis黑板持久化状态不变，系统重启后自动订阅所有核心事件Topic，消费未完成任务事件，补全缺失流转，保证无漏执行任务、无状态卡死

- Redis持久化保障：断连期间所有状态已落库，重启后完整恢复

## 5\.2 任务降级与终止策略

- **局部失败降级**：非核心子任务失败，可配置跳过，不影响整体任务完结，记录失败日志

- **全局任务终止**：核心子任务重试耗尽仍失败，顶层任务标记FAILED，终止所有后续调度，返回异常信息

- **手动取消**：支持主动取消任务，调度器标记顶层CANCELLED，终止所有子任务调度，清空运行状态

# 六、Redis黑板持久化与性能优化

## 6\.1 持久化配置（生产必开）

- 开启AOF持久化（每秒刷盘）\+ RDB定时快照（5分钟一次）

- 避免纯内存运行，进程重启、服务器断电不丢失任务状态

## 6\.2 核心优化策略

- 消息轻量化：总线消息仅传ID和元数据，大体积上下文、结果全部存储在Redis黑板

- 过期清理：幂等Key、临时快照设置过期时间，自动清理冗余数据

- 批量写入：任务拆分阶段批量写入子任务数据，减少Redis IO次数

- 读写分离：调度器写、Agent只读，杜绝并发写冲突

# 七、方案核心优势总结

4. **智能匹配调度**：基于任务类型与Agent能力画像精准分发，支持泛化匹配\+全量兜底，杜绝任务阻塞，适配多类型、多角色Agent集群协作

5. **高性能**：Redis毫秒级读写，远超传统数据库，适配高频多Agent协作场景

6. **高可用**：完整持久化、幂等、重试、超时、故障转移机制，支持断点续跑

7. **可管控**：统一调度器管控所有状态，流转清晰，可溯源、可监控、可排查故障

8. **轻量化**：无需重型MQ、数据库，部署简单，适配中小规模多Agent生产集群

# 八、系统全量核心事件清单（纯事件\+回调驱动）

本系统**无任何主动轮询、无主动巡检、无硬编码调用**，所有任务流转、状态更新、容错恢复、节点感知全部由标准化事件触发，通过回调函数闭环完成业务逻辑。所有事件依托Redis Pub/Sub总线分发，严格遵循「发布事件—订阅消费—回调更新黑板—触发下一轮事件」的闭环机制。

## 8\.1 事件整体分类规范

系统事件分为五大类：任务生命周期事件、调度驱动事件、Agent执行回调事件、故障容错事件、系统心跳运维事件，覆盖多Agent协作全链路所有动作。

## 8\.2 全量核心事件明细清单

### 8\.2\.1 任务初始化与调度驱动事件（调度器主导）

- **事件名：TASK\_CREATE\_EVENT**
触发方：调度器
订阅方：调度器（自消费）
触发时机：用户提交原始请求
核心作用：初始化顶层任务、生成全局trace\_id、写入Redis黑板顶层任务状态（PENDING）
回调闭环：自动触发规划任务下发事件

- **事件名：TASK\_PLAN\_DISPATCH\_EVENT**
触发方：调度器
订阅方：规划Agent
触发时机：顶层任务创建完成
核心作用：下发任务拆解指令，驱动规划Agent生成DAG子任务拓扑
回调闭环：规划Agent执行完成后回调 TASK\_PLAN\_FINISH\_EVENT

- **事件名：TASK\_DEPEND\_READY\_EVENT**
触发方：调度器
订阅方：调度器、执行类Agent
触发时机：某一子任务执行完成、状态更新后，DAG依赖校验通过
核心作用：通知系统存在可执行的就绪子任务，驱动新一轮任务调度
回调闭环：调度器消费事件，推送对应子任务执行事件，彻底替代轮询校验DAG

- **事件名：SUB\_TASK\_DISPATCH\_EVENT**
触发方：调度器
订阅方：对应类型业务Agent（编码/审核/汇总等）
触发时机：子任务依赖就绪、调度准入通过
核心作用：定向下发子任务执行指令，更新子任务为RUNNING、写入运行检测Key
回调闭环：Agent消费执行后，回调成功/失败事件

### 8\.2\.2 Agent执行回调事件（Agent主导，调度器消费）

- **事件名：TASK\_PLAN\_FINISH\_EVENT**
触发方：规划Agent
订阅方：调度器
触发时机：任务DAG拆分、规划逻辑执行完毕
核心作用：上报规划结果、推送完整子任务拓扑数据
回调闭环：调度器写入Redis黑板批量子任务数据，触发依赖就绪事件，开启全流程调度

- **事件名：SUB\_TASK\_SUCCESS\_EVENT**
触发方：业务执行Agent
订阅方：调度器
触发时机：子任务正常执行完成、结果合法
核心作用：上报任务输出结果、执行耗时
回调闭环：调度器更新子任务为SUCCESS、清除运行Key，触发DAG重校验与依赖就绪事件

- **事件名：SUB\_TASK\_FAIL\_EVENT**
触发方：业务执行Agent
订阅方：调度器
触发时机：LLM输出异常、业务逻辑报错、参数非法
核心作用：上报错误信息、错误码、异常堆栈
回调闭环：调度器判断重试次数，触发重试事件或最终失败事件

### 8\.2\.3 重试与故障容错事件（系统兜底）

- **事件名：SUB\_TASK\_RETRY\_EVENT**
触发方：调度器
订阅方：对应业务Agent
触发时机：子任务失败/超时、未达最大重试次数
核心作用：下发重试指令，标记任务为RETRYING
回调闭环：重试执行完成后再次回调成功/失败事件

- **事件名：SUB\_TASK\_TIMEOUT\_EVENT**
触发方：Redis Key过期事件/总线监听
订阅方：调度器
触发时机：RUNNING任务超时未回调结果
核心作用：识别僵尸任务，判定任务超时
回调闭环：调度器标记状态为TIMEOUT，自动触发重试逻辑

- **事件名：AGENT\_OFFLINE\_EVENT**
触发方：系统总线监听
订阅方：调度器
触发时机：Agent心跳中断、实例离线崩溃
核心作用：感知节点故障
回调闭环：调度器回收该节点所有运行中任务，统一触发超时重试，重新分配可用Agent

- **事件名：TASK\_FINAL\_FAIL\_EVENT**
触发方：调度器
订阅方：调度器、日志监控模块
触发时机：子任务重试耗尽、核心任务失败
核心作用：标记任务不可恢复故障
回调闭环：更新顶层任务为FAILED，终止所有后续调度，生成故障报告

### 8\.2\.4 任务收尾与终止事件

- **事件名：TASK\_ALL\_FINISH\_EVENT**
触发方：调度器
订阅方：汇总Agent、结果输出模块
触发时机：所有子任务全部执行成功
核心作用：通知系统任务整体完结
回调闭环：汇总Agent聚合所有子任务结果，调度器更新顶层任务为SUCCESS，返回用户最终结果

- **事件名：TASK\_CANCEL\_EVENT**
触发方：用户/系统运维
订阅方：调度器
触发时机：手动取消任务、前置条件失效
核心作用：主动终止任务链路
回调闭环：调度器标记顶层状态为CANCELLED，终止所有事件调度，清空运行中任务Key

### 8\.2\.5 系统运维心跳事件

- **事件名：AGENT\_HEARTBEAT\_EVENT**
触发方：所有在线Agent
订阅方：调度器
触发时机：Agent定时持续推送
核心作用：上报实例在线状态、负载信息
回调闭环：调度器实时维护在线Agent集群列表，用于负载分发、故障判定

## 8\.3 事件驱动核心约束（强规范）

- **无轮询约束**：所有DAG校验、任务就绪判断、故障检测、节点感知，100%依赖事件回调触发，禁止任何定时轮询逻辑

- **状态唯一约束**：所有事件回调后的状态写入，仅由调度器执行，Agent仅发布事件、只读黑板，不修改任何状态

- **幂等约束**：所有事件消费前置幂等校验，同一msg\_id事件仅执行一次，杜绝重复调度、重复执行

- **闭环约束**：每一类事件必须对应明确的回调处理逻辑，无孤立事件、无悬空任务

## 8\.4 标准事件流转极简闭环模板

事件发布 → 总线定向投递 → 目标Agent/调度器订阅消费 → 执行业务逻辑 → 生成结果/状态变更 → 发布新一轮回调事件 → 调度器更新黑板状态 → 触发下一轮调度事件，全程异步解耦、全自动流转

> （注：部分内容可能由 AI 生成）
